# Active Tasks

Use this file as the lightweight live tracker for repository work.

| ID | Status | Summary | Scope | Evidence | Next Step |
| --- | --- | --- | --- | --- | --- |
| ICW-309 | Done | Decouple MainViewModel from canvas state — move per-frame visible/total counts into CanvasViewModel, delete dead CanvasViewportViewModel<T>, bind header counts to CanvasSurface.ViewModel | src/InfiniteCanvas.ViewModels/CanvasViewModel.cs, src/InfiniteCanvas.ViewModels/MainViewModel.cs, src/InfiniteCanvas.ViewModels/CanvasViewportViewModel.cs (deleted), src/InfiniteCanvas.App/MainWindow.xaml, src/InfiniteCanvas.App/MainWindow.xaml.cs, tests/InfiniteCanvas.Tests/CanvasViewModelTests.cs, tests/InfiniteCanvas.Tests/CanvasViewportViewModelTests.cs (deleted), docs/tasks/tickets/ICW-309-decouple-main-view-model-from-canvas.md | CanvasViewModel owns all per-frame viewport state (ApplyFrame with viewport + visible/total counts). MainViewModel holds only settings. Dead generic CanvasViewportViewModel<T> deleted. Header counts bind to CanvasSurface.ViewModel. Release app build 0 errors; focused tests 8/8; full core suite 151/151. | Keep complete. Next step for library extraction: move CanvasControl + CanvasViewModel into a separate assembly once the app surface is stable. |
| ICW-310 | Done | Fix pan NaN by reverting exponential pan in CameraTransform to linear; keep sign-preserving exponential only in the anchor-pan input path (exponent 2.5) | src/InfiniteCanvas.Core/CameraTransform.cs, src/InfiniteCanvas.App/MainWindow.xaml.cs, tests/InfiniteCanvas.Tests/CameraTransformTests.cs, docs/tasks/tickets/ICW-310-fix-exponential-pan-nan.md | CameraTransform.Pan reverted to linear deltas (no NaN for negative deltas). Regression test added. MainWindow.ApplyDeadZone uses Sign*Pow(magnitude-deadZone, 2.5) with the dead-zone guard preserved, so magnitudes stay positive. Release build 0 errors; full core suite 151/151. | Keep complete. Anchor-pan exponential is in MainWindow; it moves to CanvasControl with the panning consolidation task. |
| ICW-206 | Done | Repair three stale tests that failed at baseline — feature percent expectations (no schema, plain double formatting), full feature row set, and scrollbar guard moved to CanvasControl after extraction | src/InfiniteCanvas.Rendering/AnnotationFeaturePresenter.cs, tests/InfiniteCanvas.Tests/AnnotationFeaturePresenterTests.cs, tests/InfiniteCanvas.Tests/CanvasScrollbarWiringTests.cs, tests/InfiniteCanvas.Tests/SampleImageGeneratorTests.cs, docs/tasks/tickets/ICW-206-repair-stale-tests.md | All three previously failing tests pass. Full core suite 149/150; the only remaining failure is the in-flight ICW-205 priority queue test (separate workstream). | Keep complete. Follow up on ICW-098 (remove orphaned MainWindow scrollbar methods). |
| ICW-204 | Done | Fix tile generation permanently lost on scroll — per-frame claimant token cancellation dropped coordinator work without resetting the tile generation-queued flag, so tiles never regenerated after scrolling (only after zoom). Reset flags on claimant-token fire and on mip failure. | src/InfiniteCanvas.Rendering/SampleImageTile.cs, tests/InfiniteCanvas.Tests/SampleImageTileTests.cs, docs/tasks/tickets/ICW-204-tile-generation-lost-on-scroll.md | Two regression tests added and passing. Core 140 passed (3 pre-existing unrelated failures), Windows 12/12, Release app build 0 errors. | Keep complete. Optional follow-up: avoid dooming in-flight work when a frame boundary creates a zero-claimant window. |
| ICW-147 | Done | Create audit-synthesis skill and evidence-first report format | .github/skills/audit-synthesis/SKILL.md, docs/formats/report-FORMAT.md, docs/tasks/tickets/ICW-147-audit-synthesis-skill-and-report-format.md | Skill and report template validated. Targeted diagnostics report no errors. Full tracker validation still reports unrelated legacy ticket schema errors and the existing ICW-148 type error. | Keep the skill and report contract available for future audit synthesis. |
| ICW-148 | Done | Register FastNoise2Bindings as a proper git submodule | .gitmodules, submodules/FastNoise2Bindings, docs/tasks/tickets/ICW-148-fastnoise2bindings-proper-submodule.md | .gitmodules mapping added, inner .git absorbed into .git/modules, vendor patches committed in submodule (fae174e). Deinit/re-update cycle and Release app build (0 errors) both pass. | Keep complete. Future FastNoise vendor changes should be committed inside the submodule and pinned via the parent gitlink. |
| (no ID - part of Sprint 1 Wave A) | Done | Preserve MainViewModel background noise settings across RegenerateSceneAsync — snapshot taken before InitializeSpatialState(), restored after | src/InfiniteCanvas.App/MainWindow.xaml.cs | Settings snapshot preserved across scene regeneration; 88 core tests pass, Release build succeeds | Keep complete. ICW-P1-SETTINGS-SCOPE covers the structural settings-validation pattern. |
| ICW-141 | Done | Plan viewport-aware tile work scheduling and cancellation — restructured as ICW-142 (coordinator), ICW-143 (viewport culling), ICW-P0*/ICW-P1* (safety/correctness harness). All implementation waves complete. | docs/ADR/0006-viewport-aware-tile-work-scheduling.md, src/InfiniteCanvas.Rendering/SampleImageTile.cs, src/InfiniteCanvas.App/MainWindow.xaml.cs, docs/tasks/tickets/ICW-141-viewport-aware-tile-work-scheduling.md | All phases implemented across Sprint 1 Waves A-D. Coordinator, viewport culling, and safety harness complete. 93 tests pass. | Restructure epic to Phase 0 (safety harness), Phase 1 (correctness), then Phase 2+ (current ICW-142/143/144 scope). See ICW-P0-* and ICW-P1-* tasks. |
| ICW-142 | Done | Add bounded cancellable tile materialization ownership | src/InfiniteCanvas.Rendering/SampleImageTile.cs, src/InfiniteCanvas.Rendering/BackgroundTileContracts.cs, src/InfiniteCanvas.Rendering/TileWorkCoordinator.cs, src/InfiniteCanvas.App/MainWindow.xaml.cs, docs/tasks/tickets/ICW-142-bounded-cancellable-tile-materialization.md | Coordinator fully wired. Critical fix: OnCoordinatorPixelsGenerated now resets _generationQueued when pixels are discarded (epoch mismatch due to cache eviction), preventing permanent stuck state. All 86 tests passing. | Keep complete. ICW-143 builds on this foundation for full viewport culling. |
| ICW-143 | Done | Add viewport culling and relevance-priority tile scheduling — ViewportInterestSet type, PublishInterestSet method on TileWorkCoordinator that cancels queued non-visible work, priority-aware DrainQueueWithLivenessCheck (visible before prefetch), interest set computation wired in RenderFrameAsync. All 93 tests pass. Post-council bug fix: PublishInterestSet no longer drops failure callbacks by removing claimants before CancelWorkItem. | src/InfiniteCanvas.App/MainWindow.xaml.cs, src/InfiniteCanvas.Rendering/SampleImageTile.cs, src/InfiniteCanvas.Rendering/TileWorkCoordinator.cs, src/InfiniteCanvas.Rendering/BackgroundTileContracts.cs, docs/tasks/tickets/ICW-143-viewport-tile-culling-and-priority.md | All P0 dependencies cleared. Viewport culling implemented: interest set published per-frame, non-visible queued work canceled, visible tiles prioritized over prefetch. Post-review bug fix and null-guard applied. 93/93 tests pass, Release build 0 errors. | Keep complete. Next: ICW-144 (fast-scroll stress benchmarks) and ICW-081 (ticket deduplication). |
| ICW-144 | In Progress | Add fast-scroll tile queue stress telemetry and benchmarks | benchmarks/InfiniteCanvas.Benchmarks/TileWorkCoordinatorBenchmarks.cs, src/InfiniteCanvas.Rendering/TileWorkCoordinator.cs, docs/tasks/tickets/ICW-144-fast-scroll-tile-queue-stress-validation.md | Seven benchmark methods cover PublishInterestSet (empty, full-visible, none-visible, mixed) and DrainQueueWithLivenessCheck (FIFO fallback, visible-promoted priority, 3-cycle fast-scroll stress). Build 0 errors, tests 93/93 pass. | Add stage-level diagnostics counters (ICW-132) to distinguish canceled vs stale vs useful completions in benchmark output. Run focused BenchmarkDotNet filter on target hardware. |
| ICW-205 | Done | Replace FIFO tile queue with a true priority queue using center-distance and mip tie-breakers | src/InfiniteCanvas.Rendering/TileWorkCoordinator.cs, src/InfiniteCanvas.Rendering/BackgroundTileContracts.cs, src/InfiniteCanvas.App/MainWindow.xaml.cs, tests/InfiniteCanvas.Tests/TileWorkCoordinatorTests.cs, benchmarks/InfiniteCanvas.Benchmarks/TileWorkCoordinatorBenchmarks.cs, docs/ADR/0006-viewport-aware-tile-work-scheduling.md, docs/requirements/functional-requirements-and-invariants.md, docs/tasks/tickets/ICW-205-priority-queue-tile-work.md | Delivered. Heap-backed PriorityQueue ordered by visibility class, squared center distance, mip suitability, then FIFO sequence. No-flash rule: visible in-flight work survives frame-token fire; stale work still canceled. Sequence-scoped tombstones so re-admitted keys are never skipped. ViewportInterestSet carries center, selected mip, and squared-distance provider; MainWindow publishes them per frame. 33/33 coordinator tests, Windows 12/12, App and benchmarks build 0 errors. Full-suite validation blocked by concurrent uncommitted ICW-309 CameraTransform refactor (unrelated camera tests fail in the tree). | Run full-suite validation after the concurrent ICW-309 work is committed, then add fast-scroll BenchmarkDotNet evidence on target hardware. |
| ICW-WAVE-F-VIEWPORT-CANCELLATION | Done | Implement Wave F viewport cancellation safety slice | src/InfiniteCanvas.Rendering/TileWorkCoordinator.cs, src/InfiniteCanvas.Rendering/SampleImageTile.cs, src/InfiniteCanvas.Rendering/SampleImageGenerator.cs, tests/InfiniteCanvas.Tests, docs/tasks/tickets/ICW-WAVE-F-VIEWPORT-CANCELLATION.md | Running cancellation now releases coordinator ownership only at worker termination; queued cancellation releases exactly once; coordinator-backed native and mip generation observes the live claimant token; token-aware overloads preserve legacy constructors. Core 95/95, Windows 10/10, Release app build 0 errors. | Keep complete. ICW-P0-LEASE-RELEASE and ICW-P1-PIXELCOST-MIPS remain the next cache-accounting prerequisites. |
| ICW-146 | Done | Show loading indicator during lazy background tile generation | src/InfiniteCanvas.App/MainWindow.xaml, src/InfiniteCanvas.App/MainWindow.xaml.cs, src/InfiniteCanvas.Rendering/SampleImageTile.cs, docs/tasks/tickets/ICW-146-tile-generation-loading-indicator.md | RenderBusyBar now shows when coordinator PendingCount > 0 (active or queued tile generation). Periodic diagnostics logging every ~120 frames logs coordinator counters, frame timing, and cache budget. All 86 tests pass. | Keep complete unless the loading indicator behavior needs tuning. |
| ICW-140 | Done | Preserve resident mip fallback during native mip generation | src/InfiniteCanvas.Rendering/SampleImageTile.cs, src/InfiniteCanvas.Rendering/ZeroCopyBitmapFactory.Windows.cs | Render path now returns the nearest resident mip while native (mip 0) generation is in flight; focused Windows tests 10/10 passed | Visual verification of mip transition during zoom and follow-up integration test |
| ICW-145 | Done | Fix IndexOutOfRangeException when mipLevel==0 falls back to lower mip without updating resident dimensions | src/InfiniteCanvas.Rendering/SampleImageTile.cs, src/InfiniteCanvas.Rendering/ZeroCopyBitmapFactory.Windows.cs, src/InfiniteCanvas.App/MainWindow.xaml.cs | When SelectMipLevel returns 0 and native pixels aren't ready, DrawTile and TryReadPixelValue used the zero-mip overload that doesn't report residentMipLevel, causing sourceDimensions to mismatch the fallback pixel array. Fixed by accepting mipLevel 0 in the mip-aware overload and always using it. All 77 tests pass. | Keep complete unless the mip-fallback contract changes. |
| ICW-130 | Done | Bake stable background-tile index text into Gray8 tile mips | src/InfiniteCanvas.Rendering/SampleImageGenerator.cs, src/InfiniteCanvas.Rendering/ZeroCopyBitmapFactory.Windows.cs, tests/InfiniteCanvas.Windows.Tests, docs/tasks/tickets/ICW-130-background-tile-index-text.md | Tile IDs are passed into generation and rasterized as value-16 Gray8 content in the same Windows GDI+ pass as circles for every requested mip; the frame-level color label pass was removed; focused Windows tests 7/7 and core generator tests 24/24 passed | Run full core/Windows suites and Release app build |
| ICW-132 | To Do | Add stage-level rendering performance instrumentation | src/InfiniteCanvas.Rendering/SampleImageGenerator.cs, src/InfiniteCanvas.Rendering/SampleImageTile.cs, src/InfiniteCanvas.Rendering/ZeroCopyBitmapFactory.Windows.cs, docs/tasks/tickets/ICW-132-rendering-performance-stage-instrumentation.md | Current capture reports `presentationframework.dll` at about 40.43% inclusive CPU, `MainWindow.RenderFrameAsync` at about 17.75%, and `DrawTile` at about 20.09%, but it cannot attribute native generation, normalization, circle rasterization, composition, cache misses, and cache hits separately. | Add low-overhead structured stage timings and cache/mip counters, then validate ICW-004 vectorization and ICW-101 lazy tooltip work with a diagnostic benchmark |
| ICW-133 | To Do | Build a stage-isolated rendering benchmark matrix | benchmarks/InfiniteCanvas.Benchmarks/TileMaterializationBenchmarks.Windows.cs, benchmarks/InfiniteCanvas.Benchmarks/ProjectionAndBitmapBenchmarks.Windows.cs, docs/tasks/tickets/ICW-133-rendering-benchmark-matrix-and-baselines.md | Existing 8192-wide result is a one-iteration Dry cold-start smoke datapoint and cannot support percentage claims or stage attribution | Add stable cold/warm, mip, reset, and stage-isolated baselines with archived machine metadata. Council 2026-08-03 (F-007): exercise the shipped tile compositor overload, not only the point-cloud path; use repeated runs for any percentage claim. |
| ICW-134 | To Do | Add variant-aware background cache accounting and reuse | src/InfiniteCanvas.Rendering/SampleImageTile.cs, src/InfiniteCanvas.Rendering/BackgroundTileContracts.cs, src/InfiniteCanvas.Rendering/ZeroCopyBitmapFactory.Windows.cs, docs/tasks/tickets/ICW-134-variant-aware-background-cache-accounting.md | Native tile payloads are already memoized, but cache reservation is tile-ID based and charges `PixelCost` (mip-0-only) rather than actual resident mip payload bytes. **Specific defects decomposed into sub-tickets:** (1) **ICW-P1-PIXELCOST-MIPS** — replace `_pixelCost` with sum-of-resident-mips method. (2) **ICW-P0-LEASE-RELEASE** — replace `ReleaseReservation` counter with `ICacheReservation : IDisposable` lease. (3) Use `ResourceKey` (including mip level) instead of tile.Id for reservations. **Depends on:** ICW-P0-LEASE-RELEASE and ICW-P1-PIXELCOST-MIPS (both must land first). | Implement variant-aware accounting after ICW-P0-LEASE-RELEASE and ICW-P1-PIXELCOST-MIPS land. Use `ResourceKey`-scoped reservations. Add `MemoryGovernor_AccountsForAllResidentMipLevelsNotJustNative` and leak-detection tests. |
| ICW-135 | Proposed | Benchmark a direct Gray8 anti-aliased circle rasterizer | src/InfiniteCanvas.Rendering/SampleImageGenerator.cs, benchmarks/InfiniteCanvas.Benchmarks/TileMaterializationBenchmarks.Windows.cs, tests/InfiniteCanvas.Tests/SampleImageGeneratorTests.cs, tests/InfiniteCanvas.Windows.Tests/ZeroCopyBitmapFactoryTests.cs, docs/tasks/tickets/ICW-135-direct-gray8-circle-rasterizer-spike.md | The only remaining plausible 8bpp opportunity is replacing the 32bpp GDI+ circle intermediate; memcpy would not preserve transparent-pixel skipping or minimum-value blending | Compare visual parity, allocations, and repeated wall time before adoption |
| ICW-131 | In Review | Review FastNoise and Gray8 pixel-transfer optimization proposals | src/InfiniteCanvas.Rendering/SampleImageGenerator.cs, submodules/FastNoise2Bindings/CSharp/FastNoise2.cs, benchmarks/InfiniteCanvas.Benchmarks/TileMaterializationBenchmarks.Windows.cs, docs/tasks/tickets/ICW-131-fastnoise-and-gray8-performance-review.md | Current synthetic path already produces Gray8 payloads; direct memcpy from the 32bpp ARGB circle intermediate is not behaviorally correct because transparent pixels are skipped and opaque values are min-blended. Direct mip generation and resident/cache reuse outrank wrapper micro-optimizations and an unbounded float-grid cache. | Run repeated target-hardware benchmark comparisons before changing the circle rasterizer, native wrapper, or cache contract |
| ICW-100 | Done | Re-apply and verify `RenderRequestTracker` wiring (ICW-078 regression) | src/InfiniteCanvas.App/MainWindow.xaml.cs, src/InfiniteCanvas.Core/RenderRequestTracker.cs, docs/tasks/tickets/ICW-078-stale-frame-epoch-guarding.md | RenderRequestTracker field added to MainWindow; BeginRequest/IsCurrent/Advance wired in RenderFrameAsync for stale-frame rejection. All 88 core tests pass. | Verify regression test persists and epoch counter is accessible to ICW-P0-STALE-PUB. |
| ICW-110 | To Do | Audit and convert `async void` handlers to safe wrappers | src/InfiniteCanvas.App/MainWindow.xaml.cs, src/InfiniteCanvas.App/App.xaml.cs, tests/InfiniteCanvas.Tests | Multiple `async void` handlers found in `MainWindow.xaml.cs` and other code-behind; these lack local exception containment and can crash the app. See docs/audits/infinitecanvaswpf-deep-dive-audit-26-07-24-07-45-28.md | Replace high-risk `async void` with `async Task` handlers where possible and provide a safe wrapper (e.g., `SafeAsyncHandler`) that logs and routes exceptions to global handlers. Add unit/integration tests to assert exceptions are caught and logged. |
| ICW-111 | To Do | Migrate annotation feature dictionary to typed `AnnotationMetrics` and update tooltip/data-grid usage | src/InfiniteCanvas.App/MainWindow.xaml.cs, src/InfiniteCanvas.App/AnnotationFeaturePresenter.cs, src/InfiniteCanvas.Rendering/SampleImageTile.cs, tests/InfiniteCanvas.Tests | Code currently reads `annotation.Features["Confidence"]` / `["Severity"]` in multiple places; brittle string keys risk runtime errors. See docs/audits/infinitecanvaswpf-audit-pass4-delta-26-07-26-15-13-15.md | Introduce a typed `AnnotationMetrics` (or value object) exposing `Confidence`/`Severity` and migration adapters. Update tooltip builder and DataGrid binding. Add tests and a migration compatibility layer. |
| ICW-112 | To Do | Expose structured TileCache diagnostics snapshot API and add throttled UI export | src/InfiniteCanvas.Rendering/TileCache.cs, src/InfiniteCanvas.Rendering/SampleImageTile.cs, src/InfiniteCanvas.App/MainWindow.xaml.cs, tests/InfiniteCanvas.Tests | Current cache diagnostics are text-only and lack variant/mip identity, reservation counts, and active-cache id. See docs/audits/icw-next-slice-delta-audit-26-07-25-05-10-00.md | Add a structured `TileCacheDiagnosticsSnapshot` type, instrument cache to populate it, expose a throttled UI command to export diagnostics, and add tests validating snapshot contents. |
| ICW-101 | Done | Make annotation tooltips safe and lazy | src/InfiniteCanvas.App/MainWindow.xaml.cs, src/InfiniteCanvas.Rendering/AnnotationFeaturePresenter.cs, src/InfiniteCanvas.Rendering/DeferredAnnotationToolTip.cs, tests/InfiniteCanvas.Tests/AnnotationFeaturePresenterTests.cs, tests/InfiniteCanvas.Tests/AnnotationTooltipWiringTests.cs, docs/tasks/tickets/ICW-101-annotation-tooltip-presenter-restore.md | Baseline profiler evidence reports 8,192 eagerly created tooltips, about 3.98% inclusive CPU in `BuildFrameVisual`, and about 3.94% self CPU in `CreateAnnotationToolTip`. The frame path now assigns a deferred presenter-backed source and removes raw feature indexers. | Focused tests pass 4/4, Windows tests pass 10/10, and the Release app build passes with the existing unused `_frameClaimantId` warning. Capture a post-change runtime profile when profiler tooling is available. |
| ICW-094 | Done | Keep the display-settings scrollbar beside, not over, panel content | src/InfiniteCanvas.App/MainWindow.xaml, tests/InfiniteCanvas.Tests/CanvasScrollbarWiringTests.cs, docs/requirements/functional-requirements-and-invariants.md, docs/tasks/tickets/ICW-094-display-settings-scrollbar-layout.md | Reserved right-side content padding and disabled horizontal overflow keep settings controls clear of the vertical scrollbar; source-level regression assertions added. App build remains blocked by pre-existing MainWindow syntax errors and task validation by older ticket metadata gaps. | Repair the unrelated existing build and tracker blockers before full end-to-end validation |
| ICW-093 | In Progress | Fix independent layer visibility and expose background target controls in the debug panel | src/InfiniteCanvas.App/MainWindow.xaml, src/InfiniteCanvas.App/MainWindow.xaml.cs, src/InfiniteCanvas.Core/CanvasUserSettings.cs, tests/InfiniteCanvas.Tests/CanvasUserSettingsTests.cs | The renderer path now separates background and sparse image layer visibility and the debug panel exposes a persisted target slider | Validate the wiring with targeted tests and a Release build once the UI path is exercised |
| ICW-094-RESET | In Progress | Harden tile reset semantics so stale in-flight generation cannot repopulate a cleared tile | src/InfiniteCanvas.Rendering/SampleImageTile.cs, tests/InfiniteCanvas.Tests/SampleImageTileTests.cs | **Duplicate ID fixed — was ICW-094 (In Progress).** A generation-epoch guard now blocks stale async work from publishing after ResetImageCache; targeted regression tests passed | Extend coverage to a full cache-reset integration path if the reset UI is exercised more heavily |
| AGT-001 | Done | Create a repo-specific agent definition and markdown task tracker | .github/agents/infinitecanvas.agent.md, docs/tasks/README.md, docs/tasks/active-tasks.md | Agent file and tracker docs created in the repository | Review the workflow with maintainers and start using it for new work |
| AGT-002 | Proposed | Review: Five small, safe, high-ROI ICW tasks selected for next-slice | docs/tasks/tickets/AGT-002-selected-small-high-ROI-tasks.md | Agent-selected candidate tasks: ICW-307, ICW-306, ICW-302, ICW-192, ICW-191 | Await user prioritization and small-PR implementation |
| ICW-008 | Done | Correct scene orientation to 2x16 and add live pixelometer world/pixel readout | src/InfiniteCanvas.App/MainWindow.xaml, src/InfiniteCanvas.App/MainWindow.xaml.cs, src/InfiniteCanvas.Rendering/SampleImageGenerator.cs, src/InfiniteCanvas.Rendering/SampleImageTile.cs, tests/InfiniteCanvas.Tests/SampleImageGeneratorTests.cs, docs/tasks/tickets/ICW-008-pixelometer-and-2x16-grid.md | Focused tests 13/13 passed and Release app project build succeeded | Follow up on deferred resize-overlay repaint behavior |
| ICW-009 | Done | Synchronize overlay and raster presentation during resize debounce | src/InfiniteCanvas.App/MainWindow.xaml, src/InfiniteCanvas.App/MainWindow.xaml.cs, docs/tasks/tickets/ICW-009-resize-overlay-sync.md | Release app build succeeded and Windows rendering tests 4/4 passed | Evaluate optional letterboxed presentation mode for aspect ratio preservation |
| ICW-010 | Done | Add red annotation style modes, defect-detail raster layer, RMB anchor-pan, and strict zoom-out clamp | src/InfiniteCanvas.App/MainWindow.xaml, src/InfiniteCanvas.App/MainWindow.xaml.cs, src/InfiniteCanvas.Rendering/SampleImageGenerator.cs, src/InfiniteCanvas.Rendering/SampleImageTile.cs, src/InfiniteCanvas.Rendering/ZeroCopyBitmapFactory.Windows.cs, tests/InfiniteCanvas.Tests/SampleImageGeneratorTests.cs, tests/InfiniteCanvas.Windows.Tests/ZeroCopyBitmapFactoryTests.cs, docs/tasks/tickets/ICW-010-annotation-modes-defect-layer-and-anchor-pan.md | Release core tests 22/22, Windows tests 4/4, and Release app build succeeded | Evaluate runtime toggle surface for animation mode and annotation render mode presets |
| ICW-011 | Done | Move defect imagery to sparse object patches, add global annotation display options, and enable axis-clamped non-uniform zoom | src/InfiniteCanvas.App/MainWindow.xaml, src/InfiniteCanvas.App/MainWindow.xaml.cs, src/InfiniteCanvas.Rendering/SampleImageGenerator.cs, src/InfiniteCanvas.Rendering/SampleImageTile.cs, src/InfiniteCanvas.Rendering/ZeroCopyBitmapFactory.Windows.cs, tests/InfiniteCanvas.Tests/SampleImageGeneratorTests.cs, tests/InfiniteCanvas.Windows.Tests/ZeroCopyBitmapFactoryTests.cs, docs/tasks/tickets/ICW-011-sparse-object-images-and-axis-clamped-zoom.md | Release core tests 22/22, Windows tests 4/4, and Release app build succeeded | Add runtime UI controls for annotation display option values |
| ICW-012 | Done | Add side-panel display controls, regenerate flow, 64-image defect pool, lazy fetched background bitmaps, and configurable X by Y tile material with fit-to-width startup | src/InfiniteCanvas.App/MainWindow.xaml, src/InfiniteCanvas.App/MainWindow.xaml.cs, src/InfiniteCanvas.Rendering/SampleImageGenerator.cs, src/InfiniteCanvas.Rendering/SampleImageTile.cs, tests/InfiniteCanvas.Tests/SampleImageGeneratorTests.cs, docs/tasks/tickets/ICW-012-configurable-material-and-display-panel.md | Release core tests 22/22, Windows tests 4/4, and Release app build succeeded | Add cancelable prefetch and generation progress metrics for deeper vertical navigation demos |
| ICW-013 | Done | Add uniform-first zoom presets/custom control, class-colored larger defects, render wait indicator, top-left class/id labels, and debug image cache dump/regeneration | src/InfiniteCanvas.App/MainWindow.xaml, src/InfiniteCanvas.App/MainWindow.xaml.cs, src/InfiniteCanvas.Rendering/SampleImageGenerator.cs, src/InfiniteCanvas.Rendering/SampleImageTile.cs, src/InfiniteCanvas.Rendering/ZeroCopyBitmapFactory.Windows.cs, tests/InfiniteCanvas.Tests/SampleImageGeneratorTests.cs, docs/tasks/tickets/ICW-013-zoom-presets-and-cache-debug.md | Release core tests 22/22, Windows tests 4/4, and Release app build succeeded | Add world-space rulers on viewport edges with tick-density scaling and camera-linked labels |
| ICW-004 | In Progress | Vectorize zoomed-out tile pixel math and measure overdraw | src/InfiniteCanvas.Rendering/ZeroCopyBitmapFactory.Windows.cs, benchmarks/InfiniteCanvas.Benchmarks/TileDrawBenchmarks.Windows.cs, benchmarks/InfiniteCanvas.Benchmarks/TileMaterializationBenchmarks.Windows.cs, benchmarks/InfiniteCanvas.Benchmarks/ProjectionAndBitmapBenchmarks.Windows.cs, tests/InfiniteCanvas.Windows.Tests/ZeroCopyBitmapFactoryTests.cs, docs/tasks/tickets/ICW-004-zoomed-out-overdraw-spike.md | Profiler capture shows `DrawTile` at about 20.09% inclusive CPU and 10.33% self CPU. Placeholder split means are 1.442 to 1.818 ms. Resident means are 8.118 to 8.467 ms. Windows exact-output coverage is 12/12. | Add stage diagnostics and vector-path reporting. For resident pixels, evaluate exact source-run processing or SIMD coordinate math. Do not add collection-marshalling libraries without evidence of a collection boundary cost. |
| ICW-005 | To Do | Define DPI-aware resize behavior and maximum surface policy | src/InfiniteCanvas.App/MainWindow.xaml.cs, src/InfiniteCanvas.Rendering, tests/InfiniteCanvas.Windows.Tests, docs/tasks/tickets/ICW-005-dpi-aware-resize-and-surface-policy.md | Cross-validated audit evidence captured for duplicated max-dimension literals | Specify policy constants and add targeted DPI and clamp tests |
| ICW-007 | To Do | Pool retained annotation overlay elements and preserve animation continuity | src/InfiniteCanvas.App/MainWindow.xaml.cs, src/InfiniteCanvas.App/MainWindow.xaml, docs/tasks/tickets/ICW-007-overlay-element-pooling.md | Cross-validated audit evidence captured for per-frame overlay allocations and animation reset behavior | Deliver as one overlay-retention slice that includes ICW-019 animation continuity and ICW-028 frame-shell retention |
| ICW-014 | In Progress | Add global unhandled exception safety net for WPF async event pipeline | src/InfiniteCanvas.App/App.xaml, src/InfiniteCanvas.App/App.xaml.cs, src/InfiniteCanvas.App/MainWindow.xaml.cs, docs/tasks/tickets/ICW-014-global-exception-safety-net.md | Global handlers now register in App startup and logging falls back to file-only if EventLog fails (verified accurate). **Expanded scope:** 19 of 21 async void handlers in `MainWindow.xaml.cs` have no `try` block — they rely entirely on the global dispatcher handler which sets `e.Handled = true` unconditionally with zero user-visible signal. This mechanism made a real crash (`BitmapConversionDuration!.Value` NullReferenceException) invisible to users. | Add non-blocking user-visible signal (status-bar message or toast) on dispatcher exceptions. Widen ICW-110 scope to include shared safe wrapper for async-void handlers. Keep EventLog fallback confirmation as verified. |
| ICW-015 | Done | Fix GenerateSet argument validation attribution and clarify parameter semantics | src/InfiniteCanvas.Rendering/SampleImageGenerator.cs, tests/InfiniteCanvas.Tests/SampleImageGeneratorTests.cs, docs/tasks/tickets/ICW-015-generateset-validation-and-parameter-semantics.md | Per-parameter validation, explicit rows/imageCount consistency, and focused regression coverage are complete; SampleImageGeneratorTests 13/13 passed | Keep complete unless the generation contract changes |
| ICW-016 | Done | Refresh README MVP behavior to match current inspection-tile implementation | README.md, docs/ADR/0002-inspection-raster-and-annotation-layers.md, docs/tasks/tickets/ICW-016-readme-mvp-behavior-refresh.md | README now documents the deterministic 2x32 tile scene, lazy generation, controls, inspection, and pan/zoom interactions | Keep complete unless the shipped MVP behavior changes. Council 2026-08-03 (C29/C44): correct README MVVM wording (IAsyncRelayCommand, RefreshCommand, IsRunning) to match the shipped orchestration. |
| ICW-017 | Done | Remove dead RefreshCommand path in CanvasViewportViewModel and make ApplyFrame canonical | src/InfiniteCanvas.ViewModels/CanvasViewportViewModel.cs, tests/InfiniteCanvas.Tests/CanvasViewportViewModelTests.cs, docs/tasks/tickets/ICW-017-refreshcommand-dead-path-removal.md | Removed test-only `RefreshCommand`/`RefreshAsync`; replaced with `ApplyFrame` behavior tests for live and non-live index services | Keep complete unless a real UI refresh affordance is later introduced |
| ICW-018 | To Do | Resolve dormant rendering abstractions and point-render overload ownership | src/InfiniteCanvas.Rendering/IRenderer.cs, src/InfiniteCanvas.Rendering/ViewportRenderRequest.cs, src/InfiniteCanvas.Rendering/ZeroCopyBitmapFactory.Windows.cs, tests/InfiniteCanvas.Windows.Tests/ZeroCopyBitmapFactoryTests.cs, benchmarks/InfiniteCanvas.Benchmarks/ProjectionAndBitmapBenchmarks.Windows.cs, docs/tasks/tickets/ICW-018-rendering-abstraction-and-overload-ownership.md | **Expanded scope:** Cross-validated audit finding: `IRenderer` and `ViewportRenderRequest` are unreferenced. Additionally, `IBackgroundTileSource` in `BackgroundTileContracts.cs` has zero implementers. Combined, 2 of 5 interfaces in the codebase have zero implementers, both in `InfiniteCanvas.Rendering`. | Decide fate of `IRenderer`, `IBackgroundTileSource`, `ViewportRenderRequest`, and 4 unused supporting records (`BackgroundTileDescriptor`, `BackgroundTileRequest`, `BackgroundTilePayload`). Options: implement, delete, or document with ADR. Council 2026-08-03 (C40): preserve IBackgroundTileSource as the ADR-0005 source-neutral boundary unless the ADR changes; removal candidates are IRenderer and ViewportRenderRequest. |
| ICW-019 | To Do | Extend overlay pooling task with animation continuity and allocation controls | src/InfiniteCanvas.App/MainWindow.xaml.cs, docs/tasks/tickets/ICW-019-overlay-animation-continuity.md | Cross-validated audit finding: selected outline animation restarts each frame because overlay elements are recreated | Treat as dependent on ICW-007 implementation so selected outline clocks persist and brush allocations are reduced |
| ICW-020 | Done | Replace pixelometer tile scan with O(1) grid lookup | src/InfiniteCanvas.App/MainWindow.xaml.cs, src/InfiniteCanvas.Core/TileGridIndexLookup.cs, tests/InfiniteCanvas.Tests/TileGridIndexLookupTests.cs, docs/tasks/tickets/ICW-020-pixelometer-o1-tile-lookup.md | `TryReadPixelValue` now computes direct tile index from scene bounds and tile dimensions; added edge-case index tests for half-open bounds and invalid inputs | Keep complete and evaluate optional blend-contract unification under ICW-035 |
| ICW-021 | To Do | Validate compositor-safe back-buffer reuse and add guard if needed | src/InfiniteCanvas.App/MainWindow.xaml.cs, src/InfiniteCanvas.Rendering/ZeroCopyBitmapFactory.Windows.cs, docs/handoffs/2026-07-23-render-coalescing.md, docs/tasks/tickets/ICW-021-backbuffer-reuse-safety.md | **Updated evidence:** External audit confirmed specific mechanism at ~70% confidence: `PublishFrame` (MainWindow.xaml.cs:465-483) recycles the just-retired front buffer as the next back buffer immediately, with no synchronization against WPF's asynchronous render-thread compositor. This is a distinct race from the defect-pool dispose finding. **Linked to:** ICW-P0-BUFFER-REUSE-SYNC (both address same InteropBitmap compositor-handoff race). | Add concrete acceptance criteria: reproduction steps, synchronization strategy (triple-buffering or explicit compositor fence), and regression test. Mark ADR-0004 Acceptance contingent on ICW-021 completion. Update ticket from template to actionable scope. |
| ICW-022 | In Progress | Extract testable logic from MainWindow code-behind and backfill unit tests | src/InfiniteCanvas.App/MainWindow.xaml.cs, src/InfiniteCanvas.App/MainWindow.xaml, src/InfiniteCanvas.App/Controls/CanvasControl.xaml, src/InfiniteCanvas.App/Controls/CanvasControl.xaml.cs, src/InfiniteCanvas.ViewModels/CanvasViewModel.cs, src/InfiniteCanvas.ViewModels, src/InfiniteCanvas.Core, tests/InfiniteCanvas.Tests, docs/tasks/tickets/ICW-022-mainwindow-decomposition-and-tests.md, docs/audits/icw-mainwindow-view-extraction-audit-26-07-25.md | CanvasControl is now the live MainWindow viewport and owns the camera surface. App Release build passes. CanvasViewModel focused tests pass 2/2 with `--no-restore`. Scrollbar jitter fix removes the competing MainWindow thumb update. Normal test restore remains blocked by the existing Rendering target mismatch. | Continue extracting scene and render orchestration from MainWindow and add control-level integration coverage. |
| ICW-102 | To Do | Implement owned disposal of bitmap pool on tile eviction/regeneration and add concurrency guard | src/InfiniteCanvas.Rendering/SampleImageTile.cs, src/InfiniteCanvas.App/MainWindow.xaml.cs, docs/tasks/tickets/ICW-102-defect-bitmap-pool-disposal.md | Audit: defect-template bitmaps are never disposed on regenerate, ICW-103 assumed a Dispose existed. **Updated scope:** `DisposeDefectTemplatePools` must be fenced against concurrent in-flight render (`RenderFrameAsync` background `Task.Run`). `_tileCoordinator.CancelAll()` alone is insufficient — it cancels coordinator work, not the render pipeline. | Expose non-destructive `WaitForIdleAsync` on `CoalescingAsyncAction` (reuse `DisposeAsync` capture-and-await pattern without setting `_disposed`). Call it from `RegenerateSceneAsync` before `DisposeDefectTemplatePools`. |
| ICW-103 | Done | Add regression test for both-axes-clamped zoom semantics in `ViewportZoomPolicy` | src/InfiniteCanvas.Core/ViewportZoomPolicy.cs, tests/InfiniteCanvas.Tests/ViewportZoomPolicyTests.cs, docs/tasks/tickets/ICW-103-viewportzoompolicy-both-clamped-test.md | Unit test added to assert both-axes-clamped behavior; `dotnet test --filter ViewportZoomPolicy` passes locally | Keep complete unless zoom policy semantics change
| ICW-104 | To Do | Define and implement deterministic tile-cache eviction policy (replace FIFO-by-dict-order) | src/InfiniteCanvas.Rendering/SampleImageTile.cs, src/InfiniteCanvas.Rendering/TileCache.cs (new), tests/InfiniteCanvas.Tests/TileCacheTests.cs, docs/tasks/tickets/ICW-104-tilecache-eviction-policy.md | Evidence: ticket `ICW-305` and audit `infinitecanvaswpf-audit-round5-26-07-25-15-03-37.md` reference FIFO-by-dictionary-order eviction and orphaned ticket | Acceptance: Implement LRU or sized-based eviction with deterministic tests; expose diagnostics counters (resident, evicted); Validation: `dotnet test tests/InfiniteCanvas.Tests/InfiniteCanvas.Tests.csproj --filter "TileCache_EvictionPolicy"` and runtime check `grep -i "evictions" logs/*.log`
| ICW-023 | To Do | Batch low-priority audit cleanups (DRY, constants, finalizer, defaults) | src/InfiniteCanvas.Rendering/SampleImageTile.cs, src/InfiniteCanvas.Core/CameraTransform.cs, src/InfiniteCanvas.ViewModels/CanvasViewportViewModel.cs, src/InfiniteCanvas.App/MainWindow.xaml, src/InfiniteCanvas.App/MainWindow.xaml.cs, src/InfiniteCanvas.Rendering/ZeroCopyBitmapFactory.Windows.cs, docs/tasks/tickets/ICW-023-low-priority-audit-cleanups.md | **Expanded scope (from external audit):** (1) `TileGridIndexLookup` `(row * columns) + column` — wrap in `checked {}` per existing convention (Bgra32BufferLayout already does this). (2) Remove dead `MipOptions` record — zero references anywhere (99% confidence, new finding). (3) Remove dead private `SampleImageGenerator.GenerateAnnotations` (lines 574-622) — byte-for-byte duplicate of `AnnotationGenerator.GenerateAnnotations`, never called. (4) Remove dead `BitmapGenerationDuration`/`BitmapConversionDuration` properties and `Gray8 {X} ms` status segment. (5) Remove redundant `fallbackScaleY` parameter from `ApplyScaleWithUniformFirst`. (6) Person-directed comments rephrase to impersonal. | Triage quick wins into one controlled cleanup with targeted tests and no behavior regression. Items 1-3 are safe to remove immediately. |
| ICW-024 | Done | Reconcile DesignDoc architecture intent with accepted ADR and task coverage | DesignDoc.md, docs/ADR/0001-benchmark-project-targeting-and-baselines.md, docs/ADR/0002-inspection-raster-and-annotation-layers.md, docs/tasks/active-tasks.md, docs/tasks/JIRA.md, docs/tasks/tickets/ICW-024-designdoc-adr-reconciliation.md | Completed gap mapping and converted missing architecture coverage into tracked items ICW-025 through ICW-027 | Keep closed; re-open only if DesignDoc scope changes materially |
| ICW-025 | In Review | Add ADR for live hybrid spatial indexing strategy already used in code | docs/ADR, src/InfiniteCanvas.Spatial/LiveSpatialIndexService.cs, src/InfiniteCanvas.Spatial/ISpatialIndexService.cs, README.md, docs/tasks/tickets/ICW-025-adr-live-hybrid-spatial-indexing.md | Proposed ADR drafted at docs/ADR/0003-live-hybrid-spatial-indexing.md | Review and approve proposal, then mark ADR Accepted |
| ICW-026 | In Review | Add ADR for zero-copy buffer lifecycle and compositor handoff policy | docs/ADR, src/InfiniteCanvas.Rendering/ZeroCopyBitmapFactory.Windows.cs, src/InfiniteCanvas.App/MainWindow.xaml.cs, docs/tasks/tickets/ICW-026-adr-zero-copy-buffer-lifecycle-policy.md | Proposed ADR drafted at docs/ADR/0004-zero-copy-buffer-lifecycle-and-handoff-policy.md | Review and approve proposal, then finalize acceptance after ICW-021 evidence |
| ICW-027 | To Do | Define GPU-pivot trigger criteria for the rendering pipeline | DesignDoc.md, benchmarks/InfiniteCanvas.Benchmarks, src/InfiniteCanvas.Rendering, docs/tasks/tickets/ICW-027-gpu-pivot-criteria-spike.md | Design open question on GPU escalation lacked dedicated backlog item | Draft measurable trigger metrics and proof-of-concept decision path |
| ICW-028 | To Do | Retain frame visual shell to reduce per-frame UI allocation churn | src/InfiniteCanvas.App/MainWindow.xaml.cs, tests/InfiniteCanvas.Windows.Tests, docs/tasks/tickets/ICW-028-retain-frame-visual-shell.md | Additional review finding: root frame `Grid`/`Image`/`Canvas` are rebuilt each frame even after annotation pooling work | Prototype persistent frame shell and compare allocation/interaction behavior |
| ICW-029 | To Do | Harden shutdown lifecycle to prevent render/regenerate disposal races | src/InfiniteCanvas.App/MainWindow.xaml.cs, src/InfiniteCanvas.App/App.xaml.cs, tests/InfiniteCanvas.Windows.Tests, docs/tasks/tickets/ICW-029-shutdown-lifecycle-race-hardening.md | Follow-up audit found race between regeneration semaphore release and shutdown disposal sequence | Introduce coordinated close sequencing and verify close-stress reliability |
| ICW-030 | Done | Define and enforce generation input bounds policy | src/InfiniteCanvas.App/MainWindow.xaml.cs, src/InfiniteCanvas.Rendering/SampleImageGenerator.cs, tests/InfiniteCanvas.Tests/SampleImageGeneratorTests.cs, docs/tasks/tickets/ICW-030-generation-input-bounds-policy.md | `MaxObjectsPerTile` is enforced in the generator and mirrored in the UI; values above 256 have focused regression coverage | Keep complete unless the generation cost model changes |
| ICW-031 | To Do | Replace string-keyed annotation feature dictionary with typed metrics | src/InfiniteCanvas.Rendering/SampleImageTile.cs, src/InfiniteCanvas.Rendering/SampleImageGenerator.cs, src/InfiniteCanvas.App/MainWindow.xaml.cs, tests/InfiniteCanvas.Tests, docs/tasks/tickets/ICW-031-typed-annotation-metrics.md | Follow-up audit found brittle runtime string-key contract for Confidence and Severity. **Concrete call site:** `CreateAnnotationToolTip` (MainWindow.xaml.cs:724) does `annotation.Features["Confidence"]`/`["Severity"]` with no `TryGetValue`. A `KeyNotFoundException` here would be silently swallowed by the global dispatcher handler. | Introduce typed metrics model and migrate all call sites including `CreateAnnotationToolTip`. |
| ICW-032 | To Do | Strengthen spatial query abstraction for count and low-allocation paths | src/InfiniteCanvas.Spatial/ISpatialIndexService.cs, src/InfiniteCanvas.Spatial, src/InfiniteCanvas.ViewModels/CanvasViewportViewModel.cs, tests/InfiniteCanvas.Tests, docs/tasks/tickets/ICW-032-spatial-query-interface-strengthening.md | Follow-up audit found list materialization is required even for count-only consumers | Add compatible count-oriented query contract and migrate count-only call sites |
| ICW-060 | **Deprecated** | Audit findings: spatial indexing subsystem (STALE — see description) | src/InfiniteCanvas.Spatial/*, docs/tasks/tickets/ICW-060-spatial-index-audit-findings.md | **STALE at HEAD.** The specific "mutable STRtree list exposure" defect was already fixed before this ticket was filed. ICW-P0-SPATIAL-INDEX-SAFETY (Done) independently confirmed and hardened the fix. Remaining concerns (boundary semantics) are tracked under ICW-033. | **Close or rescope.** If a *different* remaining concern exists (e.g., `AppendMatches` O(n) linear scan over HotItems), rewrite the ticket to describe that specific concern. Otherwise mark as Done. |
| ICW-033 | To Do | Unify boundary semantics and remove annotation placement edge bias | src/InfiniteCanvas.Core/SpatialBounds.cs, src/InfiniteCanvas.Rendering/SampleImageGenerator.cs, src/InfiniteCanvas.Rendering/SampleImageTile.cs, tests/InfiniteCanvas.Tests, docs/tasks/tickets/ICW-033-boundary-semantics-and-placement-consistency.md | Follow-up audit found closed-vs-half-open boundary mismatch and exclusive-range placement bias | Define one boundary policy, update placement math, and add edge-coordinate tests. Council 2026-08-03 (F-008): cover left, top, right, bottom, and shared-edge cases before assigning user-visible severity. |
| ICW-034 | Done | Harden coalesced render scheduler exception semantics and follow-up request preservation | src/InfiniteCanvas.Core/CoalescingAsyncAction.cs, src/InfiniteCanvas.App/MainWindow.xaml.cs, tests/InfiniteCanvas.Tests/CoalescingAsyncActionTests.cs, docs/tasks/tickets/ICW-034-coalescing-render-fault-and-followup-preservation.md | Non-cancellation scheduler faults now report through the render owner without faulting the shared task; queued follow-up requests are preserved; core tests passed 35/35 | Monitor render-fault diagnostics; ICW-014 remains the broader application-level exception safety work |
| ICW-035 | In Progress | Unify renderer and pixelometer defect blending and sampling contract | src/InfiniteCanvas.App/MainWindow.xaml.cs, src/InfiniteCanvas.Rendering/ZeroCopyBitmapFactory.Windows.cs, src/InfiniteCanvas.Rendering/SampleImageTile.cs, tests/InfiniteCanvas.Tests, tests/InfiniteCanvas.Windows.Tests, docs/tasks/tickets/ICW-035-renderer-pixelometer-blend-contract.md | A shared defect overlay sampler now powers both the pixelometer and the defect renderer, and regression tests cover the shared behavior | Extend coverage to a renderer-level pixel assertion if the defect overlay is exercised more broadly, and surface the active background tile mip level and dimensions in the pixelometer readout. Council 2026-08-03 (F-003): decide one shared overlap composition policy (max-wins or last-wins) with a contract test; do not choose without a requirement decision. |
| ICW-036 | Proposed | Establish CI and nullable-enforcement baseline | .github/workflows, Directory.Build.props, tests/InfiniteCanvas.Tests, tests/InfiniteCanvas.Windows.Tests, docs/tasks/tickets/ICW-036-ci-and-nullability-enforcement-baseline.md | Research completed: Roslyn/.NET SDK analyzers are the correct first-line tool for this all-.NET-10 WPF repository; Sonar is optional for periodic audits; Security Code Scan, Semgrep, and StyleCop are deferred due to scope, operational cost, or stale release cadence. Baseline solution build passed with no warnings. | Add centralized Roslyn configuration and Windows CI, record baseline diagnostics, then decide whether Sonar is warranted as a separate audit lane |
| ICW-138 | In Progress | Remove unnecessary null-forgiving operators in benchmark projects | benchmarks/** | Sonar flagged `!` null-forgiving operators in benchmark sources where nullable warnings are disabled; leaving suppressions hides potential NRT issues and confuses CI baselines. | Replaced low-risk `!` suppressions with nullable field initializers and removed an unused parameter in `SampleImageGenerator`; validate with build/tests and open PR |
| ICW-037 | To Do | Add accessibility baseline for MainWindow interactive controls | src/InfiniteCanvas.App/MainWindow.xaml, src/InfiniteCanvas.App/MainWindow.xaml.cs, docs/tasks/tickets/ICW-037-mainwindow-accessibility-baseline.md | Net-new audit finding: no `AutomationProperties.Name` metadata, no keyboard access keys, and no key bindings for primary actions | Add control names plus keyboard affordances for regenerate, zoom workflow, and annotation interaction without changing visual design |
| ICW-038 | To Do | Capture the net-new audit findings in a durable follow-up audit artifact | docs/audits/infinitecanvaswpf-followup-audit-26-07-24-22-24-24.md, docs/tasks/active-tasks.md, docs/tasks/JIRA.md | Follow-up audit completed and new findings were consolidated into a durable artifact for implementation planning | Keep this as a bookkeeping task until the audit artifact and task trackers are fully synchronized |
| ICW-039 | Done | Eliminate startup stalls from CPU-heavy image generation with non-blocking placeholder rendering | src/InfiniteCanvas.App/MainWindow.xaml.cs, src/InfiniteCanvas.Rendering/SampleImageGenerator.cs, src/InfiniteCanvas.Rendering/SampleImageTile.cs, src/InfiniteCanvas.Rendering/ZeroCopyBitmapFactory.Windows.cs, tests/InfiniteCanvas.Tests/SampleImageGeneratorTests.cs, tests/InfiniteCanvas.Windows.Tests/ZeroCopyBitmapFactoryTests.cs, docs/tasks/tickets/ICW-039-startup-nonblocking-shared-defect-tiles.md | Release core tests 23/23 passed, Windows tests 4/4 passed, and Release app build succeeded; first frame path now renders without blocking on background tile fetch completion | Profile startup wall time and add optional lightweight noise/pattern reintroduction only if it does not regress first-frame latency |
| ICW-040 | Done | Add a high-priority overlay layer that draws grid lines between background image tiles | src/InfiniteCanvas.App/MainWindow.xaml.cs, docs/tasks/tickets/ICW-040-background-tile-grid-overlay.md | Dedicated camera-synchronized grid canvas added; full tests 32/32 passed and Release app build succeeded | Keep grid styling configurable only if runtime review requires it |
| ICW-041 | Done | Correct annotation label sizing and class-or-ID display selection | src/InfiniteCanvas.App/MainWindow.xaml, src/InfiniteCanvas.App/MainWindow.xaml.cs, docs/tasks/tickets/ICW-041-label-size-and-display-mode.md | Default changed from 12 to 8.5; Class/ID selector defaults to Class; full tests 32/32 passed and Release app build succeeded | Validate label density visually at extreme zoom-out |
| ICW-042 | Done | Rebuild sparse defect imagery as unaltered grayscale System.Drawing.Bitmap inputs | src/InfiniteCanvas.Rendering/SampleImageGenerator.cs, src/InfiniteCanvas.Rendering/SampleImageTile.cs, src/InfiniteCanvas.Rendering/ZeroCopyBitmapFactory.Windows.cs, tests/InfiniteCanvas.Windows.Tests/ZeroCopyBitmapFactoryTests.cs, docs/tasks/tickets/ICW-042-grayscale-bitmap-defect-layer.md | Windows pixel test proves grayscale 150 is preserved outside logical annotation bounds; full tests 32/32 passed | Add explicit bitmap-pool disposal ownership when regeneration lifecycle is hardened under ICW-029 |
| ICW-043 | Done | Persist display and generation settings across app runs and save on close | src/InfiniteCanvas.Core/CanvasUserSettings.cs, src/InfiniteCanvas.App/MainWindow.xaml.cs, tests/InfiniteCanvas.Tests/CanvasUserSettingsTests.cs, docs/tasks/tickets/ICW-043-persist-settings-on-close.md | JSON round-trip and malformed-file fallback tests passed; full tests 32/32 passed and Release app build succeeded | Extend version migration only when the persisted schema changes |
| ICW-044 | Done | Correct independent axis zoom clamping and derived zoom percentage display | src/InfiniteCanvas.Core/ViewportZoomPolicy.cs, src/InfiniteCanvas.App/MainWindow.xaml, src/InfiniteCanvas.App/MainWindow.xaml.cs, tests/InfiniteCanvas.Tests/ViewportZoomPolicyTests.cs, docs/tasks/tickets/ICW-044-axis-clamp-and-derived-zoom-display.md | Focused independent-clamp and largest-axis display tests passed; full tests 32/32 passed and Release app build succeeded | Visually verify wheel anchoring when one axis is clamped |
| ICW-045 | Done | Replace the removed custom zoom UI with a better integrated entry | src/InfiniteCanvas.App/MainWindow.xaml, src/InfiniteCanvas.App/MainWindow.xaml.cs, docs/tasks/tickets/ICW-045-custom-zoom-entry-redesign.md | `Custom...` reveals a percent field; Apply and Enter share validation and camera-update behavior; Release app build succeeded | Keep complete unless future UX review calls for a different preset surface |
| ICW-046 | Done | Restore uniform zoom as soon as an axis-clamped viewport permits it | src/InfiniteCanvas.Core/ViewportZoomPolicy.cs, tests/InfiniteCanvas.Tests/ViewportZoomPolicyTests.cs, docs/tasks/tickets/ICW-046-uniform-zoom-recovery.md | Focused `ViewportZoomPolicyTests` passed 5/5 and Release app build succeeded | Visually verify cursor anchoring across the recovery transition |
| ICW-047 | Done | Implement taller noisy background tiles with pixel-budget cache controls | src/InfiniteCanvas.App/MainWindow.xaml, src/InfiniteCanvas.App/MainWindow.xaml.cs, src/InfiniteCanvas.Rendering/SampleImageGenerator.cs, src/InfiniteCanvas.Rendering/SampleImageTile.cs, src/InfiniteCanvas.Rendering/ZeroCopyBitmapFactory.Windows.cs, tests/InfiniteCanvas.Tests/SampleImageGeneratorTests.cs | Verified by core tests 32/32 passed, Windows tests 5/5 passed, and the Release build succeeded; tiles now render noisy sparse backgrounds and cache status updates in the UI | Optionally tune visual noise intensity and cache-threshold values during runtime review |
| ICW-048 | Done | Show annotation feature values in a sidebar DataGrid on selection | src/InfiniteCanvas.App/MainWindow.xaml, src/InfiniteCanvas.App/MainWindow.xaml.cs, src/InfiniteCanvas.Rendering/SampleImageTile.cs, tests/InfiniteCanvas.Tests/SampleImageGeneratorTests.cs | Verified by core tests 32/32 passed, Windows tests 5/5 passed, and the Release build succeeded; selection now populates the feature grid with formatted values and an empty state | Optionally add richer feature formatting or sort controls if inspection workflows expand |
| ICW-049 | Done | Fix tile-cache budget capacity regression | src/InfiniteCanvas.App/MainWindow.xaml.cs, src/InfiniteCanvas.Rendering/SampleImageGenerator.cs, src/InfiniteCanvas.Rendering/SampleImageTile.cs, tests/InfiniteCanvas.Tests/SampleImageGeneratorTests.cs, docs/requirements/functional-requirements-and-invariants.md | Default cache budget now retains four 8192x4096 tiles (134,217,728 pixels); focused cache tests passed 7/7 and Release app build succeeded | Monitor runtime diagnostics to confirm tiles remain resident after generation |
| ICW-050 | Done | Make lazy tile generation deterministic and concurrency-safe from a master seed | src/InfiniteCanvas.Rendering/SampleImageGenerator.cs, tests/InfiniteCanvas.Tests/SampleImageGeneratorTests.cs, docs/requirements/functional-requirements-and-invariants.md, docs/tasks/tickets/ICW-050-deterministic-threadsafe-tile-generation.md | Local deterministic streams now derive from the master seed; bounded sparse noise replaces 33.5M per-tile RNG calls; core tests 36/36, Windows tests 5/5, and Release app build passed | Profile the release scene only if sparse-noise density needs visual tuning |
| REQ-001 | Done | Capture recurring functional requirements and invariants in a durable registry | docs/requirements/functional-requirements-and-invariants.md, docs/tasks/active-tasks.md, docs/tasks/JIRA.md | Added a canonical requirements registry for zoom, tile, cache, annotation, and settings invariants so they remain visible alongside ADRs and tickets | Keep this registry updated as new behavioral requirements are introduced |
| ICW-064 | Done | Bound lazy tile-cache admission without evicting visible tiles | src/InfiniteCanvas.Rendering/SampleImageTile.cs, src/InfiniteCanvas.Rendering/ZeroCopyBitmapFactory.Windows.cs, src/InfiniteCanvas.App/MainWindow.xaml.cs, tests/InfiniteCanvas.Tests/SampleImageGeneratorTests.cs, docs/tasks/tickets/ICW-064-tile-cache-capacity-and-materialization-metrics.md | Replaced after-the-fact pixel eviction with a 4 GiB byte admission ceiling, viewport pinning, rejected-request placeholders, and failed-generation reservation release; cache diagnostics now show GiB capacity, resident tiles, evictions, and visible backgrounds; focused tests passed 13/13 and the Release app build succeeded | Profile cache hit rate and materialization alternatives only after this bounded baseline has runtime soak evidence. Council 2026-08-03 (F-002): prove queued work is not silently evicted, or that eviction cancels work and releases ownership exactly once. |
| ICW-070 | Proposed | Add a read-only camera scroll-position indicator | src/InfiniteCanvas.App/MainWindow.xaml, src/InfiniteCanvas.App/MainWindow.xaml.cs, docs/tasks/tickets/ICW-070-readonly-viewport-scroll-indicator.md | User requested visible scroll-level feedback after the native scrollbar rollback | Draw a camera-native indicator that never affects viewport layout or zoom calculations |
| ICW-071 | Proposed | Move annotation features into a collapsible dockable sidebar | src/InfiniteCanvas.App/MainWindow.xaml, src/InfiniteCanvas.App/MainWindow.xaml.cs, docs/tasks/tickets/ICW-071-dockable-annotation-feature-sidebar.md | User requested a future separate feature-inspection sidebar | Design the collapsible sidebar boundary after the cache fix is stable |
| ICW-065 | Done | Add camera-native viewport scrollbars and preserve fixed camera sizing | src/InfiniteCanvas.Core/ViewportScrollbarPolicy.cs, src/InfiniteCanvas.App/MainWindow.xaml, src/InfiniteCanvas.App/MainWindow.xaml.cs, tests/InfiniteCanvas.Tests/ViewportScrollbarPolicyTests.cs, docs/tasks/tickets/ICW-065-viewport-scrollbars-and-zoom-navigation.md | Custom overlay tracks and thumbs support click-to-position and dragging through `CameraTransform`; focused policy tests passed 3/3 and the Release app build succeeded without reintroducing `ScrollViewer` layout sizing | Extend the custom controls only through camera-native behavior; preserve the fixed-viewport invariant |
| ICW-066 | Done | Expose background tile noise and defect-circle tuning through runtime settings | src/InfiniteCanvas.Rendering/SampleImageGenerator.cs, src/InfiniteCanvas.Core/CanvasUserSettings.cs, src/InfiniteCanvas.App/MainWindow.xaml, src/InfiniteCanvas.App/MainWindow.xaml.cs, tests/InfiniteCanvas.Tests/SampleImageGeneratorTests.cs, docs/tasks/tickets/ICW-066-configurable-background-noise-tuning.md | Added persisted background-noise and defect-circle sliders that regenerate the scene immediately; verified by 39/39 core tests and a Release build | Keep this as a completed control-surface improvement and use it as the basis for the debug editor slice |
| ICW-067 | In Progress | Build reusable SliderTextBox controls and organize generator settings into expanders | src/InfiniteCanvas.App/MainWindow.xaml, src/InfiniteCanvas.App/Controls/SliderTextBox.xaml, src/InfiniteCanvas.App/Controls/SliderTextBox.xaml.cs, src/InfiniteCanvas.App/Controls/TileBackgroundNoiseSettingsView.xaml, src/InfiniteCanvas.App/Controls/TileBackgroundNoiseSettingsView.xaml.cs, src/InfiniteCanvas.ViewModels/MainViewModel.cs, tests/InfiniteCanvas.Tests | Research confirmed the noise editor still uses repeated label and Slider markup, the tile X/Y and other generation fields remain raw TextBoxes, and the tile settings have no expander. The noise expander header uses the accent color instead of a lighter text color. | Implemented `SliderTextBox` control and `BoundedNumeric` parsing; migrated noise controls and replaced generation TextBoxes with `SliderTextBox` inside a `SCENE MATERIAL` expander. Core tests pass. See ICW-151 for data-context lifecycle follow-up. |
| ICW-150 | In Progress | Make background generation and cache accounting mip-aware | src/InfiniteCanvas.Rendering/SampleImageTile.cs, src/InfiniteCanvas.Rendering/BackgroundTileContracts.cs, src/InfiniteCanvas.Rendering/ZeroCopyBitmapFactory.Windows.cs, src/InfiniteCanvas.App/MainWindow.xaml.cs, tests/InfiniteCanvas.Tests, tests/InfiniteCanvas.Windows.Tests, docs/tasks/tickets/ICW-150-mip-aware-background-cache-accounting-and-generation.md | Viewport interest set now requests only the selected mip. Tile cache reservations are keyed by `BackgroundTileCacheKey` with per-mip byte costs and `ICacheReservation` release semantics wired through coordinator cancel and failure paths. Focused validation passed: core 57/57, Windows 10/10, app Release build succeeds. | Run a fast zoom stress pass to verify stale/superseded completion release behavior and review diagnostics readability for variant-heavy scenes. |
| ICW-151 | Done | Preserve background-noise control state across scene regeneration | src/InfiniteCanvas.App/MainWindow.xaml.cs, src/InfiniteCanvas.ViewModels/MainViewModel.cs, src/InfiniteCanvas.Rendering/SampleImageGenerator.cs, tests/InfiniteCanvas.Tests/MainViewModelTests.cs, docs/tasks/tickets/ICW-151-preserve-noise-settings-viewmodel-on-regeneration.md | `MainWindow` now keeps one `MainViewModel` for the window lifetime so `RegenerateSceneAsync` no longer replaces the bound settings state. Noise defaults are centralized on `CanvasUserSettings`, octaves default drift is removed, and regression coverage now verifies all eight noise fields survive regeneration snapshots. | Keep ICW-067 focused on reusable control refactors only. Preserve the stable view-model lifecycle for settings invariants. |
| ICW-188 | To Do | Introduce GeneratorOptions and MipOptions records for generator API | src/InfiniteCanvas.Rendering/GeneratorOptions.cs, src/InfiniteCanvas.Rendering/MipOptions.cs, src/InfiniteCanvas.Rendering/SampleImageGenerator.cs, docs/tasks/tickets/ICW-188-introduce-generator-and-mip-options.md | Existing ticket is registered in JIRA but was absent from this tracker. Current HEAD contains both option records and option-based generator usage, while migration parity and direct-caller evidence remain open. | Confirm direct MipOptions callers and add parity tests before closure. |
| ICW-189 | To Do | Add GenerateSet(GeneratorOptions) adapter and preserve deprecation forwarding | src/InfiniteCanvas.Rendering/SampleImageGenerator.cs, docs/tasks/tickets/ICW-189-add-generateSet-adapter-and-deprecation-path.md | Existing ticket is registered in JIRA but was absent from this tracker. Adapter, forwarding, XML documentation, and parity acceptance remain separate from the option-record task. | Verify forwarding behavior and tile-ID and pixel parity before closure. |
| ICW-068 | Proposed | Add an about/licensing dialog for third-party credits and project attribution | src/InfiniteCanvas.App/MainWindow.xaml, src/InfiniteCanvas.App/MainWindow.xaml.cs, docs/tasks/tickets/ICW-068-about-and-licensing-dialog.md | The request included a discoverable licensing page and copied license assets | Ship a lightweight About dialog and link to the relevant license text bundle |
| ICW-069 | Proposed | Turn the recent requests and handoff notes into a phased sprint plan of high-ROI tasks and important fixes | docs/tasks/tickets/ICW-069-phased-sprint-plan.md, docs/tasks/active-tasks.md | The latest handoff and human requests now map into a concrete execution plan covering interaction polish, stability, and rendering performance | Use ICW-069 as the sequencing guide for the next implementation batches |
| ICW-052 | Proposed | Extract testable logic from MainWindow and add unit tests | src/InfiniteCanvas.App/MainWindow.xaml.cs, src/InfiniteCanvas.App/ViewportZoomCalculator.cs (new), src/InfiniteCanvas.App/GenerationOptionsValidator.cs (new), tests/InfiniteCanvas.Tests/ViewportZoomCalculatorTests.cs (new) | `MainWindow` contains >1000 lines mixing pure logic and UI wiring; no unit tests cover zoom/generation math | Extract pure helpers, add unit tests, and update `MainWindow` to call them |
| ICW-053 | Done | Remove dead RefreshCommand path in CanvasViewportViewModel | src/InfiniteCanvas.ViewModels/CanvasViewportViewModel.cs, tests/InfiniteCanvas.Tests/CanvasViewportViewModelTests.cs, docs/tasks/tickets/ICW-053-refreshcommand-dead-path-removal.md | Removed unused command path and migrated tests to `ApplyFrame`-centric assertions | Track future command-surface decisions only if a new UI requirement appears |
| ICW-054 | Proposed | Pool annotation overlay elements and preserve selection animation continuity | src/InfiniteCanvas.App/MainWindow.xaml.cs, src/InfiniteCanvas.App/MainWindow.xaml | Per-frame allocation of overlay UI elements causes animation restart and allocation churn | Implement pooling keyed by annotation id and preserve Shape instances for selected outlines |
| ICW-055 | Done | Replace pixelometer linear tile scan with O(1) grid lookup | src/InfiniteCanvas.App/MainWindow.xaml.cs, src/InfiniteCanvas.Core/TileGridIndexLookup.cs, tests/InfiniteCanvas.Tests/TileGridIndexLookupTests.cs, docs/tasks/tickets/ICW-055-pixelometer-o1-lookup.md | Replaced linear tile scan with direct grid index lookup and added border/out-of-range tests | Keep complete; fold any further pixelometer work into ICW-035 and ICW-022 |
| ICW-072 | Reverted | Preserve the uniform width-clamped zoom round trip | src/InfiniteCanvas.Core/ViewportZoomPolicy.cs, tests/InfiniteCanvas.Tests/ViewportZoomPolicyTests.cs, docs/requirements/functional-requirements-and-invariants.md, docs/tasks/tickets/ICW-072-zoom-notch-continuity-at-axis-floors.md | Reverted the incorrect independent-floor behavior; the intentional `xIsClamped || yIsClamped` policy remains in place | Continue with ICW-014 stability work |
| ICW-073 | Done | Separate runtime show settings for labels, boxes, sparse image tiles, and background images | src/InfiniteCanvas.App/MainWindow.xaml, src/InfiniteCanvas.App/MainWindow.xaml.cs, src/InfiniteCanvas.Core/CanvasUserSettings.cs, src/InfiniteCanvas.Rendering/ZeroCopyBitmapFactory.Windows.cs, tests/InfiniteCanvas.Tests/CanvasUserSettingsTests.cs, docs/tasks/tickets/ICW-073-separate-show-settings.md | Independent persisted toggles now cover labels, sparse image tiles, and background images; background-image visibility is included in the settings record and save/load path | Keep complete and use as the baseline for future runtime display-tuning work |
| ICW-076 | In Progress | Add source-agnostic background tile mip-level fetching for zoomed-out views | src/InfiniteCanvas.Rendering/BackgroundTileContracts.cs, src/InfiniteCanvas.Rendering/SampleImageGenerator.cs, src/InfiniteCanvas.Rendering/SampleImageTile.cs, src/InfiniteCanvas.Rendering/ZeroCopyBitmapFactory.Windows.cs, tests/InfiniteCanvas.Tests/SampleImageGeneratorTests.cs, docs/ADR/0005-source-agnostic-background-tile-mips.md, docs/tasks/tickets/ICW-076-background-tile-mip-levels.md | Added canonical source/revision/mip contracts, eight-level ceiling policy, deterministic low-pass synthetic reduction, per-mip tile generation, and camera-selected Windows raster sampling; focused core tests 25/25 and Windows raster tests 5/5 passed. Nonzero mip variants still need materializer/cache reservation, variant pinning, and external-source coalescing. | Move nonzero mip admission and completion ownership into the source-neutral materializer/cache, then add variant-budget and zoom-transition raster regressions. Council 2026-08-03 (F-001): own the ADR-0005 pixelometer acquisition migration. Add a cold-hover test proving no coordinator work is submitted when a mip is absent. |
| ICW-077 | Proposed | Harden viewport scrollbar overlay geometry and initialization state | src/InfiniteCanvas.App/MainWindow.xaml.cs, src/InfiniteCanvas.App/MainWindow.xaml, docs/tasks/tickets/ICW-077-viewport-scrollbar-overlay-hardening.md | Net-new audit found nullable initialization and geometry assumptions around the viewport scrollbar overlay | Implement an explicit initialization guard and add regression coverage for partially measured overlay state. Council 2026-08-03 (C32): fold the repeated axis-selection ternaries into shared helpers in the same slice. |
| ICW-078 | Done | Guard render and regeneration paths against stale frame publication | src/InfiniteCanvas.App/MainWindow.xaml.cs, src/InfiniteCanvas.Core/RenderRequestTracker.cs, tests/InfiniteCanvas.Tests/RenderRequestTrackerTests.cs, docs/handoffs/2026-07-25-render-stability-sprint-handoff.md, docs/tasks/tickets/ICW-078-stale-frame-epoch-guarding.md | Current HEAD contains RenderRequestTracker.BeginRequest, IsCurrent, and Advance in the render path. The ticket and JIRA status now match the source evidence. | Keep complete. Rerun the focused render-request tests when the next rendering change touches this path. |
| ICW-079 | Done | Make busy-state updates and render coalescing resilient to rapid input churn | src/InfiniteCanvas.App/MainWindow.xaml.cs, src/InfiniteCanvas.Core/CoalescingAsyncAction.cs, src/InfiniteCanvas.Core/CameraTransform.cs, tests/InfiniteCanvas.Tests/CameraTransformTests.cs, tests/InfiniteCanvas.Tests/CanvasScrollbarWiringTests.cs, docs/requirements/functional-requirements-and-invariants.md, docs/tasks/tickets/ICW-079-render-busy-state-coalescing.md | LMB drag and RMB anchor-pan no longer invalidate the active render on every pointer event; intentionally wide camera scale defaults are preserved and documented; scrollbar overlay, handlers, policy use, and render update hook are guarded; core tests 51/51, Windows tests 5/5, and Release app build passed | Keep complete; future input-churn or UI cleanup work must preserve the non-blocking coalesced render path and scrollbar wiring contract |
| ICW-080 | Proposed | Extract annotation feature formatting from MainWindow into a dedicated presentation model | src/InfiniteCanvas.App/MainWindow.xaml.cs, src/InfiniteCanvas.Rendering/SampleImageTile.cs, docs/tasks/tickets/ICW-080-annotation-feature-presentation-model.md | Net-new audit found the feature-grid and tooltip formatting logic are tightly coupled to MainWindow and hard to test independently | Move formatting and selection-view logic into a small presentation model with focused tests |
| ICW-081 | In Progress | Reconcile duplicate and orphaned audit tickets before adding more backlog work | docs/tasks/tickets, docs/tasks/active-tasks.md, docs/tasks/JIRA.md, scripts/Validate-TaskTracker.ps1, docs/audits/infinitecanvaswpf-critical-peer-review-26-07-25.md | Current ticket corpus still contains duplicate identities, incompatible schemas, tracker status divergence, and existing ICW-188/189 ticket files absent from this live tracker. The validator reports legacy metadata failures but does not yet enforce all identity and cross-surface rules. | Complete one inventory across ticket files, active-tasks.md, and JIRA.md. Register ICW-188/189, preserve duplicate evidence, and extend validation without creating duplicate IDs. |
| ICW-082 | Done | Persist the background-image visibility toggle | src/InfiniteCanvas.Core/CanvasUserSettings.cs, src/InfiniteCanvas.App/MainWindow.xaml.cs, tests/InfiniteCanvas.Tests/CanvasUserSettingsTests.cs, docs/tasks/tickets/ICW-082-persist-background-image-visibility.md | `ShowBackgroundImagesCheckBox` now round-trips through settings persistence and is applied during render and startup load | Keep complete and use this as the compatibility baseline for future layer-visibility work |
| ICW-084 | To Do | Refactor `scripts/Validate-TaskTracker.ps1` to reduce cognitive complexity | scripts/Validate-TaskTracker.ps1 | Sonar flagged a function with cognitive complexity 22; refactor into smaller helpers and add Pester tests | Extract front-matter parsing helpers, add tests, and reduce the function complexity below 15 |
| ICW-085 | To Do | Clean up nullable suppression operators and duplicate handlers in `MainWindow.xaml.cs` | src/InfiniteCanvas.App/MainWindow.xaml.cs | Sonar flagged null-forgiving operators and duplicate method bodies in multiple spots | Replace `!` with proper initialization/guards, merge duplicate handlers into shared helpers, and consider converting obvious instance methods to `static` where safe |
| ICW-086 | To Do | Replace `await CancelAsync` patterns with `CancelAsync` in async disposals and handlers | src/InfiniteCanvas.App/MainWindow.xaml.cs, src/InfiniteCanvas.Core/CoalescingAsyncAction.cs | Sonar suggests avoiding awaiting CancelAsync in some disposal paths | Review cancellation patterns, update code, and add regression tests |
| ICW-087 | To Do | Reduce cognitive complexity and simplify nested ternary in `ViewportZoomPolicy.cs` | src/InfiniteCanvas.Core/ViewportZoomPolicy.cs | High cognitive complexity and nested ternary at critical clamp logic | Extract named locals or small helpers and add unit tests to ensure identical behavior |
| ICW-088 | To Do | Reduce parameter count and unnecessary casts in `SampleImageGenerator` methods | src/InfiniteCanvas.Rendering/SampleImageGenerator.cs | Methods with up to 11 parameters and redundant cast to byte flagged by Sonar | Introduce `GeneratorOptions` and remove unnecessary casts; update tests |
| ICW-089 | To Do | Review `ISpatialIndexService<T>` variance and query contract | src/InfiniteCanvas.Spatial/ISpatialIndexService.cs | Sonar suggests adding `out` variance to `T` for API flexibility | Audit implementations and add interface-level tests or document rationale in an ADR |
| ICW-090 | To Do | Audit and reduce methods with too many parameters | src/**/* | Sonar found several methods exceeding the parameter threshold | Refactor highest-impact methods into small config objects or helpers |
| ICW-091 | To Do | Simplify defect template pool creation and remove redundant casts | src/InfiniteCanvas.Rendering/SampleImageGenerator.cs | Redundant casts and complex defect pool creation detected | Simplify template pool code and add integrity tests |
| ICW-092 | To Do | Replace manual loops with LINQ `.Where` in spatial index implementations where appropriate | src/InfiniteCanvas.Spatial/* | Sonar suggests simplifying loops for readability | Replace loops with `.Where` where performance is not harmed and document choices |
| ICW-305 | Proposed | Make tile-cache eviction policy explicit and testable | src/InfiniteCanvas.Rendering/SampleImageTile.cs, tests/InfiniteCanvas.Tests, docs/tasks/tickets/ICW-305-tilecache-eviction-policy.md | Existing ticket file is orphaned from both trackers and confirms eviction selects the first eligible dictionary value rather than a documented recency policy | Register the ticket, choose/document eviction semantics, and add deterministic policy tests |
| ICW-096 | In Review | Restore viewport scrollbars and preserve resident imagery during mip transitions | src/InfiniteCanvas.App/MainWindow.xaml, src/InfiniteCanvas.App/MainWindow.xaml.cs, src/InfiniteCanvas.Rendering/SampleImageTile.cs, src/InfiniteCanvas.Rendering/ZeroCopyBitmapFactory.Windows.cs, docs/tasks/tickets/ICW-096-scrollbars-and-resident-mip-fallback.md | Historical camera-native scrollbar overlay restored; direct resident mip fallback added; focused core tests 24/24, Windows tests 5/5, and Release app build passed. Cache-variant diagnostics remain a follow-up. | Review the visual transition and implement variant-aware cache accounting before closing |
| ICW-098 | In Progress | Enforce resident-mip fallback for rendering during mip transitions | src/InfiniteCanvas.Rendering/SampleImageTile.cs, src/InfiniteCanvas.Rendering/ZeroCopyBitmapFactory.Windows.cs, src/InfiniteCanvas.App/MainWindow.xaml.cs, tests/InfiniteCanvas.Tests | `DrawTile` should prefer the best resident payload even when generation is gated, and the fallback should prefer the nearest resident mip rather than jumping straight to native level 0. See docs/audits/icw-delta-audit-26-07-25-04-20-00.md. | Implement unconditional resident-fallback selection in the render path, keep the fallback ordered by mip distance with a higher-resolution bias, and add regression tests that assert a closer resident mip remains visible during mip transitions. |
| _(see ICW-P1-SETTINGS-VALIDATION)_ | Proposed | Thread `MinimumSparseTilePixelSize` setting into render pipeline and UI (moved to ICW-P1-SETTINGS-VALIDATION scope) | src/InfiniteCanvas.Core/CanvasUserSettings.cs, src/InfiniteCanvas.App/MainWindow.xaml, docs/tasks/tickets/ICW-P1-SETTINGS-VALIDATION.md | **Duplicate — subsumed by ICW-P1-SETTINGS-VALIDATION.** This is Instance 2 of the settings-validation gap pattern. | Delete this row when ICW-P1-SETTINGS-VALIDATION is implemented. |
| ICW-P0-STALE-PUB | Done | Add explicit stale-generation publication guard at OnCoordinatorPixelsGenerated with shared epoch mechanism (ContentRevision vs _generationEpoch). Injection test CoordinatorCompletion_WithStaleEpoch_DiscardsPixels verifies stale coordinator completions are discarded after ResetImageCache. All 91 tests pass. | src/InfiniteCanvas.Rendering/SampleImageTile.cs, src/InfiniteCanvas.Rendering/TileWorkCoordinator.cs, docs/audits/external-audit-master-synthesis-26-07-30.md | External auditor (Integration-1 Phase 0) required that stale tile generation results be rejected at the completion callback. Frame-level epoch guard (ICW-078/ICW-100) existed; tile-level guard was unverified. Added explicit stale-generation publication guard at OnCoordinatorPixelsGenerated with shared epoch mechanism (ContentRevision vs _generationEpoch). Injection test CoordinatorCompletion_WithStaleEpoch_DiscardsPixels verifies stale coordinator completions are discarded after ResetImageCache. All 91 tests pass. | Keep complete. |
| ICW-P0-ACTIVECOUNT | Done | Verify `_activeCount` decrement is in worker termination path | src/InfiniteCanvas.Rendering/TileWorkCoordinator.cs, docs/audits/external-audit-master-synthesis-26-07-30.md | **Verified at Sprint 1 Wave A:** `_activeCount` decremented in worker termination path only, not in `CancelWorkItem`. No changes needed. | Keep complete. Two residual issues surfaced (see ICW-P0-ACTIVECOUNT-residuals). |
| ICW-P0-ACTIVECOUNT-residuals | Proposed | Close two residual issues from ICW-P0-ACTIVECOUNT fix | src/InfiniteCanvas.Rendering/TileWorkCoordinator.cs, docs/tasks/tickets/ICW-P0-ACTIVECOUNT-residuals.md | **Residual A (85% confidence):** `CancelWorkItem` calls `ReleaseReservation` for running items, and `HandleWorkStopped` also calls it — double-count today, double-dispose hazard after ICW-P0-LEASE-RELEASE. **Residual B (85% confidence):** `_items.Remove(key)` at cancel-request time (not physical worker exit) admits duplicate worker for same tile key. Safe (epoch guards discard stale results) but wastes CPU. | **Residual A:** Move `_items.Remove(key); ReleaseReservation(key);` into queued-item branch only. Running items rely on `HandleWorkStopped`. **Residual B:** Document as accepted tradeoff at the `_items.Remove(key)` call. Add no tracking state. Must land before ICW-P0-LEASE-RELEASE. |
| ICW-P0-QUEUE-DRAIN | Done | Add claimant-token liveness check in DrainQueue — Phase 0 (method skeleton + CancellationToken.None placeholder + 2 focused tests) and Phase 1 (real ClaimantCount==0 check that cancels stale items instead of promoting them). All 88 tests pass. | src/InfiniteCanvas.Rendering/TileWorkCoordinator.cs, docs/audits/external-audit-master-synthesis-26-07-30.md, docs/audits/implementation-sequencing-review-26-07-30.md | Both phases complete. Phase 0: method skeleton + CancellationToken.None placeholder + 2 focused tests. Phase 1: real ClaimantCount==0 liveness check cancels stale items instead of promoting them. All 88 tests pass. | Keep complete. |
| ICW-P0-PIXELOMETER-READOUT | Done | Pass _tileCacheBudget.TryReserve through pixelometer TryReadPixelValue path so hover-triggered tile generation participates in cache budget accounting. Long-term conversion to published-frame snapshot deferred. | src/InfiniteCanvas.App/MainWindow.xaml.cs, src/InfiniteCanvas.Rendering/SampleImageTile.cs, docs/audits/external-audit-master-synthesis-26-07-30.md | **Interim fix applied:** TryReadPixelValue now passes _tileCacheBudget.TryReserve through pixelometer path. Hover-triggered tile generation participates in cache budget accounting. All 88 tests pass. Long-term published-frame snapshot conversion deferred. | Keep complete. |
| ICW-P0-TRANSACTIONAL-REGEN | Proposed | Add transactional guard for `RegenerateSceneAsync` with fallback to previous scene | src/InfiniteCanvas.App/MainWindow.xaml.cs, docs/tasks/tickets/ICW-P0-TRANSACTIONAL-REGEN.md | **Critical gap (85% confidence):** If `RegenerateSceneAsync` throws or cancels mid-way, the app is left with partially-initialized state. Snapshot previous scene state before mutation; wrap generation+publish in try/catch; restore on failure. **Depends on:** ICW-102 (defect bitmap pool disposal fencing — rollback needs undisposed pools). | 1) Snapshot `_tiles`, `_annotations`, `_sceneBounds`, `_spatialIndex`, `_mainViewModel` before mutation. 2) Wrap generation+publish in try/catch. 3) On failure, restore snapshots and show error status. 4) On `OperationCanceledException` during shutdown, let propagate without rollback. 5) Integration test: inject GenerateSet that throws, assert previous scene preserved. See ticket for full plan. |
| ICW-P0-BUFFER-REUSE-SYNC | Proposed | Add synchronization or triple-buffering for `InteropBitmap` compositor handoff | src/InfiniteCanvas.App/MainWindow.xaml.cs, docs/tasks/tickets/ICW-P0-BUFFER-REUSE-SYNC.md, docs/ADR/0004-zero-copy-buffer-lifecycle-and-handoff-policy.md | **Critical gap (85% confidence):** `PublishFrame` recycles retired front buffer as next back buffer immediately with no synchronization against WPF compositor. Classie double-buffering race where the "free" buffer is recycled before the consumer (WPF compositor) has finished reading it. Affects every frame. | **Option A (recommended):** Triple-buffering — keep 3 `ZeroCopyBitmapFactory` instances in rotation, one extra at viewport resolution (~64 MiB at 4096x4096). **Option B:** Explicit compositor fence via `CompositionTarget.Rendering`. Add reference-counting wrapper in test builds. Update ADR-0004. See ticket for full plan. |
| ICW-P0-SPATIAL-INDEX-SAFETY | Done | LiveSpatialIndexService.Query now returns ToArray() instead of mutable List<T> for full snapshot isolation during publish. Added Query_ReturnsImmutableArray_CallerCannotModify (verifies Array type) and QueryDuringPublish_ReturnsConsistentSnapshot contract tests. 91/91 tests pass. | src/InfiniteCanvas.Spatial, docs/audits/external-audit-master-synthesis-26-07-30.md | External auditor Phase 0 required spatial index safety. ICW-060 (Proposed) and ADR-0003 (In Review) existed but were unstarted. Query returned mutable List<T> that callers could modify during publish. Changed to ToArray() for full snapshot isolation. Added Query_ReturnsImmutableArray_CallerCannotModify (verifies Array type) and QueryDuringPublish_ReturnsConsistentSnapshot contract tests. 91/91 tests pass. | Keep complete. |
| ICW-P0-LEASE-RELEASE | Proposed | Replace `ReleaseReservation` counter with `IDisposable` lease pattern | src/InfiniteCanvas.Rendering/TileWorkCoordinator.cs, src/InfiniteCanvas.Rendering/SampleImageTile.cs, docs/tasks/tickets/ICW-P0-LEASE-RELEASE.md | **Critical (98% confidence):** `ReleaseReservation` is `Interlocked.Increment(ref _reservationReleases)` — a diagnostic counter with **no connection** to `TileCacheBudget.UsedBytes`. Budget bytes leak on every cancellation/eviction/frame-supersession that bypasses `OnTilePixelsGenerationFailed`. **Depends on:** ICW-P0-ACTIVECOUNT-residuals (fix double-ReleaseReservation first), ICW-P1-PIXELCOST-MIPS (lease releases wrong byte count if _pixelCost is mip-0-only). | 1) Introduce `ICacheReservation : IDisposable`. 2) Return from `TryReserve` instead of `bool`. 3) Route every cancel/fail/reject path through lease `Dispose()`. 4) Fix residual double-call in `CancelWorkItem` (running-item branch must not call ReleaseReservation — only HandleWorkStopped does). 5) Add leak-detection and double-dispose-safety tests. See ticket for full plan. |
| ICW-P0-SEQUENCING | Proposed | Restructure ICW-141 epic with Phase 0 and Phase 1 milestones | docs/tasks/active-tasks.md, docs/ADR/0006, docs/tasks/tickets/ICW-P0-SEQUENCING.md | Current task sequence (ICW-142 → 143 → 144) has no Phase 0 safety harness. External auditor requires Phase 0 (coordinator fixes, cache accounting) before Phase 1 (claimant tokens, cooperative cancellation) before current scope. ICW-142/143 are Done; ICW-144 is Proposed. | Restructure ICW-141 epic into three phases. Update ADR-0006 with phase structure. Document completion criteria per phase. Update ICW-141 ticket to reference restructured phases. See ticket for full phase inventory. |
| ICW-P0-MIGRATION-GUARD | Proposed | Add concurrent-development policy for 8-phase migration | docs/ADR/0006, docs/tasks/tickets/ICW-P0-MIGRATION-GUARD.md | Codebase is under rapid active iteration with multiple agents. No documented strategy for managing concurrent feature work during P0/P1 migration. Without a guard, agents could independently modify same subsystem with conflicting changes. | Add explicit policy to ADR-0006: Option A (recommended for P0) — freeze demo-app feature work during P0 safety fixes. Option B — strangler-fig pattern (new work against emerging contracts). Add migration-state row to active-tasks.md. Add PR checklist for concurrent-change awareness. See ticket for full plan. |
| ICW-P1-CLAIMANT-TOKENS | Done | Wire per-frame/viewport CancellationToken into coordinator claimant API — per-tile claimant identity replaces static DefaultCoordinatorClaimant; ClaimantTokenProvider provides per-frame token; per-frame CTS replacement in RenderFrameAsync with deferred two-frame disposal. 88/88 tests pass. | src/InfiniteCanvas.App/MainWindow.xaml.cs, src/InfiniteCanvas.Rendering/SampleImageTile.cs, src/InfiniteCanvas.Rendering/TileWorkCoordinator.cs, docs/audits/external-audit-master-synthesis-26-07-30.md | All 88 core tests pass. Release build succeeds. | Keep complete. |
| ICW-P1-COOPERATIVE-CANCEL | Proposed | Add cancellation checks in tile generation factories around each expensive sub-phase | src/InfiniteCanvas.Rendering/SampleImageGenerator.cs, src/InfiniteCanvas.Rendering/SampleImageTile.cs, docs/tasks/tickets/ICW-P1-COOPERATIVE-CANCEL.md | **Critical gap (90% confidence):** Factory delegates receive a real `CancellationToken` (since ICW-P1-CLAIMANT-TOKENS), but never check it. Native factory (lines 420-431) ignores token entirely. Mip factory (lines 546-551) checks only post-hoc. `SampleImageGenerator` methods have no token parameter or check. During fast scroll, GDI+ and noise work continues to completion after tile leaves viewport. **Depends on:** ICW-P1-CLAIMANT-TOKENS (prerequisite — now done, tokens are real). | 1) Add `CancellationToken` parameter to `ApplyDetailsWithGdiPlus`, `GenerateMonochromeMipPixels`, `GenerateNoisePixelsCore`. 2) Add `ThrowIfCancellationRequested()` before/after each expensive sub-phase. 3) Fix native factory asymmetry (add token checks). 4) Add injection test for mid-execution cancellation responsiveness. See ticket for full plan. |
| ICW-P1-GDI-CONCURRENCY | Proposed | Add explicit GDI+ concurrency management for tile generation factories | src/InfiniteCanvas.Rendering/SampleImageGenerator.cs, docs/tasks/tickets/ICW-P1-GDI-CONCURRENCY.md | **Critical gap (65% confidence — mechanism real, crash not reproduced):** `ApplyDetailsWithGdiPlus` constructs `System.Drawing.Bitmap`/`Graphics` per call, invoked concurrently from up to `DefaultMaxConcurrency = 4` workers. .NET GDI+ wrapper has known thread-affinity issues. Additionally, ICW-P0-ACTIVECOUNT residual B (duplicate admission) creates >maxConcurrency concurrent GDI+ calls. **Depends on:** ICW-P0-ACTIVECOUNT, ICW-P1-COOPERATIVE-CANCEL (shorter GDI+ calls reduce overlap window). | **Option A (recommended):** Serialize `ApplyDetailsWithGdiPlus` behind `SemaphoreSlim(1,1)` — simple, safe, immediately testable. **Option B:** Dedicated GDI+ worker thread with work queue. Add stress test running maxConcurrency concurrent calls in tight loop. Document duplicate-admission interaction in code comment. See ticket for full plan. |
| ICW-P1-SETTINGS-VALIDATION | Proposed | Create single validation function per option field used by all entry paths | src/InfiniteCanvas.Core/CanvasUserSettings.cs, src/InfiniteCanvas.App/MainWindow.xaml.cs, src/InfiniteCanvas.Rendering/SampleImageGenerator.cs, docs/audits/external-audit-master-synthesis-26-07-30.md | **Pattern-level fix:** Two known instances: (1) `ObjectsPerTile` upper bound absent from `CanvasUserSettings.IsValid` — a settings file with `ObjectsPerTile = 500` passes load validation and crashes on first generate. (2) `MinimumSparseTilePixelSize` validated and stored but never consumed. | Add `ObjectsPerTile <= MaxObjectsPerTile` to `IsValid`. Thread `MinimumSparseTilePixelSize` into `GenerateFrozenBitmap`. Create single validation function per field shared by XAML, JSON, and host-adapter paths. Add test that would catch either regression. Council 2026-08-03 (F-006): produce a canonical noise-default ownership table (NoiseOctaves differs across four locations), validate the BackgroundNoise upper bound, and address unbounded numeric text boxes. |
| ICW-P1-PIXELCOST-MIPS | Proposed | Replace `_pixelCost` with sum of all resident mip payload bytes | src/InfiniteCanvas.Rendering/SampleImageTile.cs, docs/tasks/tickets/ICW-P1-PIXELCOST-MIPS.md | **Critical gap (95% confidence):** `_pixelCost = checked(pixelWidth * pixelHeight)` is computed once from mip-0 dimensions only, never revised. `TileCacheBudget.UsedBytes` undercounts actual heap by up to ~33% once multiple mip levels accumulate. `ReleaseReservation` (ICW-P0-LEASE-RELEASE) is untrustworthy until this is fixed, since it would release the wrong byte count. **Must land together with ICW-P0-LEASE-RELEASE.** | Replace `_pixelCost` with `ResidentByteCount` property summing all resident mip payload bytes under `_cacheGate`. Update `TileCacheBudget.TryReserve` and `Release` to use new property. Update eviction policy to use accurate byte counts. Add `MemoryGovernor_AccountsForAllResidentMipLevelsNotJustNative` test. See ticket for full plan. |
| ICW-P1-SETTINGS-SCOPE | Proposed | Add explicit settings-bug acceptance criteria to Phase 1 of compatibility plan | src/InfiniteCanvas.App/MainWindow.xaml.cs, docs/audits/external-audit-master-synthesis-26-07-30.md | The compatibility plan's Phase 1 does not mention settings-persistence or ViewModel-lifecycle bugs. A team could complete all phases and ship known bugs intact. | Add to ICW-022 (MainWindow decomposition) Phase 1 acceptance criteria: (a) settings not silently reset on `RegenerateSceneAsync`, (b) `IsValid` checks all enforced bounds, (c) every persisted setting is verifiably consumed. |
| ICW-100 | To Do | Define overlay precedence and align pixelometer sampling with rendered mip — **RETAINED as unique ticket** | src/InfiniteCanvas.Rendering/DefectOverlaySampler.cs, src/InfiniteCanvas.App/MainWindow.xaml.cs, docs/tasks/tickets/ICW-100-overlay-precedence-and-pixelometer.md `DefectOverlaySampler.ResolveDisplayValue` uses last-wins order; pixelometer sampling reads native payload while renderer may show a resident mip. This is a distinct concern from RenderRequestTracker (Done). | Define explicit precedence and either sample the same resident mip as the renderer or make pixelometer label sampling semantics explicit. Add contract tests and renderer/pixelometer integration tests. Council 2026-08-03 (C22): keep this distinct from RenderRequestTracker; resolve the overlap composition policy open question before closure. |
| ICW-129 | Done | Migrate background noise generation to FastNoise2 | src/InfiniteCanvas.Rendering/SampleImageGenerator.cs, src/InfiniteCanvas.Rendering/InfiniteCanvas.Rendering.csproj, src/InfiniteCanvas.App/InfiniteCanvas.App.csproj, tests/InfiniteCanvas.Tests/InfiniteCanvas.Tests.csproj, tests/InfiniteCanvas.Tests/SampleImageGeneratorTests.cs, docs/tasks/tickets/ICW-129-fastnoise2-background-noise.md | FastNoise2 native DLL and bindings are now integrated into the rendering/runtime project graph, and the generator uses deterministic wordspace-origin sampling via FastNoise2’s uniform-grid API. | Keep this task closed; continue monitoring the FastNoise2 binding warnings and any visual tuning needed for the new noise look. |
| ICW-097 | In Review | Remove avoidable synthetic RGB-to-Gray8 materialization | src/InfiniteCanvas.Rendering/SampleImageGenerator.cs, src/InfiniteCanvas.Rendering/SampleImageTile.cs, tests/InfiniteCanvas.Windows.Tests/ZeroCopyBitmapFactoryTests.cs, benchmarks/InfiniteCanvas.Benchmarks, docs/profiler-results/Report20260725-1202.diagsession, docs/tasks/tickets/ICW-097-cpu-pixel-processing-optimization.md | The 60.5%-CPU scalar `Format24bppRgb` to Gray8 conversion was removed; the generator now also uses pooled FastNoise samples, native min/max, affine noise mapping, and fast sign-aware float rounding. Current Dry benchmark datapoint: one 8192x4096 tile 675.742 ms / 32 MiB; four 8192-wide tiles 2.348 s / 128 MiB. Core generator tests 24/24 and Release app build passed. | Capture a repeated before/after benchmark on the target machine before claiming a percentage improvement; then prioritize ICW-076 variant-aware cache accounting and diagnostics |
| ICW-098-scrollbar | Proposed | Finish or remove partially landed viewport scrollbar slice (was ICW-098 duplicate) | src/InfiniteCanvas.App/MainWindow.xaml, src/InfiniteCanvas.App/MainWindow.xaml.cs, docs/tasks/tickets/ICW-098-finish-or-remove-scrollbar-slice.md | **Duplicate ID fixed — was ICW-098 (Proposed).** `MainWindow.xaml.cs` contains `UpdateViewportScrollbars` and mouse handlers but `MainWindow.xaml` lacks the named scrollbar controls; render path does not call the update method. | Decide: (A) finish end-to-end scrollbar or (B) remove orphan methods. Add regression test for `UpdateViewportScrollbars` wiring. |
| ICW-099 | **Deprecated** | Harden Serilog EventLog sink initialization (STALE — see description) | src/InfiniteCanvas.App/Logging/SerilogHost.cs, docs/tasks/tickets/ICW-099-serilog-eventlog-startup-fallback.md | **STALE at HEAD (75% confidence).** External audit confirmed `SerilogHost.CreateLogger()` already wraps `WriteTo.EventLog(...)` in try/catch with file-only fallback at `SerilogHost.cs:34-41`. **Residual uncertainty (25%):** Not verified whether the admin-rights check can occur lazily on first write outside the try block. | Verify (via spike or source reading) whether EventLog admin-rights check is synchronous (covered by existing try/catch) or lazy (needs expanded try/catch). If synchronous, close ticket. |
| ICW-081 | Done | Reconcile duplicate and orphaned ticket IDs in `docs/tasks/tickets/` (was ICW-100 duplicate) | docs/tasks/tickets/, docs/tasks/JIRA.md, scripts/Validate-TaskTracker.ps1, docs/tasks/tickets/ICW-081-audit-ticket-corpus-reconciliation.md | **Duplicate ID fixed — was ICW-100 (Proposed).** Inventory shows duplicated IDs across multiple files causing tracker divergence. Duplicate ICW-094 entry fixed (ICW-094-RESET). ICW-098/099/100 duplicates already resolved with dedup notes in active-tasks.md. ICW-102 dup and ICW-111/ICW-031 merge documented. Validation script updated to flag duplicate IDs. | Keep complete. If new duplicate IDs are discovered, update the validation script and resolve individually. |
| _(see ICW-022)_ | To Do | Decompose `MainWindow.xaml.cs` orchestration into testable services (was ICW-100 duplicate) | src/InfiniteCanvas.App/MainWindow.xaml.cs, docs/tasks/tickets/ICW-022-mainwindow-decomposition-and-tests.md | **Duplicate ID fixed — was ICW-100 (To Do).** Subsumed by ICW-022 (MainWindow decomposition). MainWindow >1k lines mixing generation, render scheduling, camera policy, etc. | Delete this row when ICW-022 is implemented. |
| ICW-101 | To Do | Make `SampleImageGenerator.GenerateSet` shape contract explicit — **RETAINED as unique ticket** | src/InfiniteCanvas.Rendering/SampleImageGenerator.cs, tests/InfiniteCanvas.Tests/SampleImageGeneratorTests.cs | Audit: `GenerateSet` accepts `imageCount` and `rows` and silently prefers grid shape when `rows` is supplied, causing implicit contract confusion. | Split into `GenerateSetByCount(...)` and `GenerateSetByGrid(columns, rows, ...)` or rename to make precedence explicit. Add regression tests for precedence semantics |
| ICW-111 | To Do | Replace string-keyed annotation feature dictionary with typed metrics | src/InfiniteCanvas.Rendering/SampleImageTile.cs, src/InfiniteCanvas.Rendering/SampleImageGenerator.cs, src/InfiniteCanvas.App/MainWindow.xaml.cs, tests/InfiniteCanvas.Tests, docs/tasks/tickets/ICW-111-annotation-metrics-migration.md, docs/tasks/tickets/ICW-031-typed-annotation-metrics.md | **Duplicate of ICW-031.** Audit: `CreateAnnotationToolTip` and callers read `annotation.Features["Confidence"]`/`["Severity"]` directly; runtime KeyNotFound or type errors possible if keys differ. **Depends on:** ICW-101 (tooltip presenter restore — eliminates crash risk before refactoring). | Merge with ICW-031. Introduce typed `AnnotationMetrics` record, add to `SampleAnnotation`, migrate all call sites, deprecate `Features` dictionary. See ICW-031 ticket for full plan. Fix duplicate ID. |

## Sprint 1 Wave A — Viewport Safety Harness

Completed 2026-07-30:
- ICW-100 (Done): RenderRequestTracker wiring re-applied
- ICW-P0-QUEUE-DRAIN Phase 0 (Done): DrainQueue liveness skeleton + tests
- Background noise settings reset (Done): MainViewModel preserved across RegenerateSceneAsync

All items validated with dotnet test (88/88 passing) and Release build.

## Sprint 1 Wave B — Viewport Correctness Harness

Completed 2026-07-30:
- ICW-P1-CLAIMANT-TOKENS (Done): Per-tile claimant identity + per-frame CancellationToken wiring
- ICW-P0-QUEUE-DRAIN Phase 0+1 (Done): DrainQueue liveness skeleton + real ClaimantCount check
- ICW-P0-PIXELOMETER-READOUT (Done): Cache budget wired through pixelometer path

All items validated with dotnet test (88/88 passing) and Release build.

## Sprint 1 Wave C — Clear ICW-143 Dependencies

Completed 2026-07-30:
- ICW-P0-STALE-PUB (Done): Explicit stale tile-publication guard + injection test
- ICW-P0-SPATIAL-INDEX-SAFETY (Done): Immutable Query results + contract tests

All items validated with dotnet test (91/91 passing) and Release build.

## Sprint 1 Wave D — Viewport Culling (ICW-143)

Completed 2026-07-30:
- ICW-143 (Done): ViewportInterestSet, PublishInterestSet, priority-aware drain, wired in RenderFrameAsync
- Post-council bug fix (2026-07-30): PublishInterestSet no longer removes claimants before CancelWorkItem — failure callbacks were silently dropped, causing permanent tile stuck state
- Post-council null guard (2026-07-30): ViewportInterestSet constructor validates non-null arguments
- Post-council defense-in-depth (2026-07-30): Added _disposed guard to DrainQueueWithLivenessCheck

All items validated with dotnet test (93/93 passing) and Release build.

See docs/audits/sprint1-wave-d-council-review-26-07-30.md for full council findings.
See docs/handoffs/2026-07-30-sprint1-wave-d-supplementary-review.md for next-agent handoff.

## Sprint 1 Wave E — Post-Wave Cleanup, Deduplication, and Stress Benchmarks

Completed 2026-07-30:
- Tracker updates: ICW-143 ticket status verified Done, ICW-P0-ACTIVECOUNT verified Done, Wave D invariants already present in requirements registry
- ICW-094 duplicate fixed: tile-reset concern assigned ICW-094-RESET sub-ID
- ICW-081 (Done): Ticket deduplication — duplicate ICW-094 entry resolved, ICW-098/099/100 duplicates already resolved with dedup notes, ICW-111/ICW-031 merge documented, validation script updated
- ICW-144 (In Progress): TileWorkCoordinatorBenchmarks.cs added with 8 benchmark scenarios covering PublishInterestSet (empty, full-visible, none-visible, mixed) and DrainQueueWithLivenessCheck (FIFO fallback, visible-promoted priority, 3-cycle fast-scroll stress)

All items validated with dotnet test (93/93 passing) and Release build (0 errors, 0 warnings).

See docs/handoffs/2026-07-30-sprint1-wave-e-complete.md for handoff and next steps.
